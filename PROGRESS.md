# PROGRESS.md — Build Tracker (kept by humans and agents)

**How this file is used:** every coding agent works through the **current sub-phase's** checkboxes top-to-bottom — one sub-phase at a time (each is sized ≈ half a day). Tick a box only when it is *verifiably* true (run the thing). Never skip acceptance criteria. When a sub-phase's boxes are all ticked, set **Current phase** to the next one. Milestones (M0–M6) are umbrella groupings — their **Milestone gate** is checked when every sub-phase inside is done. If you deviate from `AGENTS.md`/`STACK.md`/`docs/`, append a dated line to the **Devlog** at the bottom explaining what and why.

**Current phase:** One-day MVP SHIPPED (2026-09-10, section below) — next: M4 proper. M3.7's manual dress rehearsal is superseded by the MVP run-through; M4.1–M4.3 endpoints exist in MVP-cut shapes (docs/05 "MVP cut" section) and need upgrading to the frozen contracts.

**Never-cut items** (thesis-critical, dropped only by explicit human decision): M3 trust loop · eval harness (M4/M6) · snapshot/undo.

---

## M0 — Skeleton

*Goal: the three moving parts exist and talk to each other.*

### M0.1 — Electron scaffold
- [x] `app/` Electron project scaffolds with electron-vite; `npm run dev` opens a window on Windows

**Done when:** the app window opens on Windows with no errors in the dev console.

### M0.2 — First Thread + pinned trio
- [x] Renderer renders one assistant-ui `Thread` with a hello-world runtime
- [x] `ai` / `@assistant-ui/react` / `@assistant-ui/react-ai-sdk` resolved **together** and pinned in the lockfile; the three versions logged in the Devlog (the bridge package dictates the `ai` version — downgrade `ai` first if they clash)

**Done when:** a hello-world exchange renders in the Thread and the three resolved versions are in the Devlog.

### M0.3 — Real IPC transport (canned stream)
- [x] The Thread runs on the **real custom IPC transport already** — main echoes a canned multi-part stream over IPC (no model key needed); stream parts cross verbatim (docs/02 §2.1)

**Done when:** the canned multi-part stream renders in the Thread exactly as main emitted it.

### M0.4 — Sidecar boots
- [x] `services/intelligence/` uv project boots; `GET /health` returns `{"status":"ok"}` on `127.0.0.1:7891` with a valid `X-Agento-Token`; tokenless requests are refused with 403 (docs/05 §1)

**Done when:** a request with the token returns ok and the same request without it returns 403.

### M0.5 — Sidecar lifecycle in main
- [x] Main process spawns the sidecar on app start, generates the per-launch random token and sends it as `X-Agento-Token` on every request (docs/02 §2.4), and forwards `/health` status to the renderer (visible as a status dot)

**Done when:** launching the app spawns the sidecar, the status dot turns green once healthy, and sidecar calls carry the per-launch token.

### M0.6 — DB gate (better-sqlite3)
- [x] **DB gate:** better-sqlite3 opens under Electron — one-query smoke test (`select 1`, WAL) green; if the ABI mismatch appeared, `@electron/rebuild` postinstall added; Devlog records prebuild vs rebuild (docs/08 §7)

**Done when:** the smoke test passes under Electron and the Devlog says which world you're in (prebuild or rebuild).

### M0.7 — Pipelines green
- [x] `npm run typecheck && npm run lint` green; `uv run pytest` green (1 smoke test)

**Done when:** every listed command exits 0.

**Milestone gate:** all boxes ticked and the window shows a chat UI with a green sidecar dot.

## M1 — Chat on real models

*Goal: streaming conversations against a real provider, persisted.*

### M1.1 — Settings + API key
- [x] Settings screen: add API key (Google AI Studio recommended first), pick model; key stored via Electron `safeStorage`

**Done when:** an entered key persists across an app restart and is only readable in the main process.

### M1.2 — IPC chat transport on real models
- [x] Custom IPC chat transport: renderer `useChat` ↔ main-process `streamText` (AI SDK v5 UI message stream over IPC)

**Done when:** the first real streaming response renders incrementally in the Thread.

### M1.3 — Sessions + sidebar
- [x] Sessions in SQLite (schema from `docs/03-agent-core.md` §8); sidebar lists and reopens them

**Done when:** a conversation survives an app restart and reopens from the sidebar. *(Met: 3-restart CDP sequence — run1 20/20, restart run2 9/9, restart run3 10/10, zero renderer console errors; DB dump verified; report `phase-reports/M1.3-sessions-sidebar.md`.)*

*Note: `sessions.workspace_path` is `NOT NULL` (docs/03 §8) — until the workspace picker lands (M2.2), store an empty-string placeholder and record the convention in the Devlog.*

### M1.4 — Stop button
- [x] Stop button aborts the run (`AbortController` wired end-to-end)

**Done when:** stopping mid-stream leaves a clean, resumable session. *(Met: 4-run CDP sequence — run1 20/20 (stop during the reasoning lead-in AND during visible text: native `{type:'abort'}` terminal, no error part, composer re-enables), restart run2 10/10 (partial assistant reply restored from storage; both stopped sessions resumable), run3 10/10 (double-send on new chat = ONE row/one stream; second send during an active run = one run on the wire), run4 10/10 (mid-stream switch aborts the old run and persists its partial), zero renderer console errors every run; DB dump verified (0001 UNIQUE(session_id, seq), no dupes); report `phase-reports/M1.4-stop-button.md`.)*

### M1.5 — Token usage
- [x] Token usage recorded and shown per session

**Done when:** usage numbers show for the session after a chat. *(Met: run1 20/20 — Lighthouse run shows `242 in · 867 out` on its sidebar row with the first `agent:event` carrying the full envelope (`seq=1`, per-send `runId`), Bridges updates only its own row (`241 in · 1,287 out`) with a fresh per-session envelope, and a stopped run records honestly nothing (verified against the installed ai@5.0.250: `streamText`'s `onFinish` never fires on a mid-first-step abort and `result.totalUsage` rejects — no row, no event, nothing fabricated); run2 5/5 — full quit + relaunch restores totals identically from `usage_events`; db-dump ALL CHECKS PASSED (migration 0002 exact columns, two rows keyed by session, sums match the sidebar, stopped session absent); zero renderer console errors in both runs; typecheck + lint green, pytest 3 passed; sidebar totals owned by the settle-triggered session:list refresh (hardened after review — no provisional event adds); report `phase-reports/M1.5-token-usage.md`.)*

### M1.6 — Design-system foundation *(docs/04 §8.1–8.2)*
- [x] Tailwind v4 + shadcn/ui initialized (CSS-first config, `components.json`) — both already in STACK.md
- [x] shadcn token variables with light + dark sets (dark default; the user-facing switcher arrives with M6.3); existing chrome (SessionsSidebar, SettingsDialog, thread, composer) restyled onto tokens with no behavior changes
- [x] Inter self-hosted (`@fontsource-variable/inter`) — bundled by Vite, no network fetch, no CSP change
- [x] Styled scrollbars on every scroll region (thread viewport, sessions list, settings body, composer textarea) — no native Chromium scrollbar visible anywhere
- [x] `npm run typecheck && npm run lint` green

**Done when:** the app matches docs/04 §7/§8 tokens with styled scrollbars in every scroll region, checked in both themes (light via temporarily flipping the variable set). *(Met: Tailwind 4.3.3 + `@tailwindcss/vite` 4.3.3 + Inter 5.3.0 + cn() deps landed with STACK.md rows in the same commit, pinned ai/assistant-ui trio untouched; `base.css` retired, `main.css` is the CSS-first v4 entry with neutral sets + indigo `--primary`/`--ring` + semantic `--success`/`--warning`, dark default via `<html class="dark">`, every existing class name kept so TSX/behavior is untouched; 8px token-themed scrollbars on all four regions with `scrollbar-gutter: stable`; functional1 12/12 (stream `235 in · 89 out`, stop records nothing), functional2 15/15 (usage restored identically, settings key save/remove cycle with `secrets.bin` hash-verified backup/restore), visual 37/37 (dark default, light flip, reload restore, zero external requests, zero console errors), typecheck + lint green, pytest 3 passed; report `phase-reports/M1.6-design-system-foundation.md`.)*

### M1.7 — Transcript polish *(docs/04 §8.3–8.4)*
- [x] Assistant replies render as markdown via `@assistant-ui/react-markdown` + `remark-gfm` (headings, lists, tables, links; inline code + code blocks monospace per §7)
- [x] Streaming "thinking" indicator while a reply runs and no text has arrived yet (covers the reasoning-model lead-in) — compose from assistant-ui primitives first; if not covered, adopt prompt-kit's Loader / Thinking-Bar under the amended docs/04 §4 ladder and log the adoption in §4
- [x] Scroll-to-bottom button in the thread when scrolled up — assistant-ui primitive if covered, else prompt-kit's Scroll Button (which brings `use-stick-to-bottom`), logged in docs/04 §4
- [x] ~150ms message enter transition + hover states; `prefers-reduced-motion` disables all added motion
- [x] `npm run typecheck && npm run lint` green

**Done when:** a markdown-rich reply renders fully styled, with the thinking indicator and scroll-to-bottom button visible during a live stream. *(Met: `@assistant-ui/react-markdown` 0.11.9 + `remark-gfm` 4.0.1 installed exact (version gate honored — pinned `@assistant-ui/react` 0.11.56 untouched; 0.11.9 is the highest markdown package whose peer range `^0.11.53` accepts it), CDP main run 39/39 — real h2/ul/table/pre DOM in `.message--assistant .message-markdown`, user messages stay plain, code header "code — JavaScript · copy" with clipboard round-trip verified, thinking indicator shows during the reasoning lead-in and disappears on first text, scroll-to-bottom appears on scroll-up during the lead-in and on idle threads, click jumps to bottom and hides, view follows streaming at bottom, both named buttons + links achromatic on the neutral `--primary` token, zero external requests, zero console errors; reduced run 8/8 (`prefers-reduced-motion: reduce` emulated — dots/message animations report `none`, streaming unaffected); restore run 6/6 (fresh launch, motion forced back on — `animation-name: message-in` active); db-dump: schema/migrations unchanged from M1.5, assistant rows store raw text-only `{type,text}` parts with markdown syntax intact (no HTML), usage still records natural-finish runs only; typecheck + lint green, pytest 3 passed; report `phase-reports/M1.7-transcript-polish.md`.)*

**Milestone gate:** a conversation streams, survives an app restart, and stopping mid-stream leaves a clean, resumable session. *(Met by construction — every M1.* sub-phase box is ticked; M1.4 verified stop mid-stream persists a clean partial, M1.5 verified restart restores sessions/usage, M1.6/M1.7 verified the polished transcript.)*

## M2 — The agent touches files

*Goal: the thin backend becomes an agent with safe file powers.*

### M2.1 — Tool registry + wrapper
- [x] Tool registry with the wrapper: validate → sandbox → risk → approval-hook → **snapshot** → execute (`docs/03-agent-core.md` §5, §7), proven on a first tool with unit tests

**Done when:** the wrapper's unit tests pass on a tool that exercises every stage. *(Met: first `npm test` in `app/` — vitest 5.0.0 exact-pinned (STACK.md row already existed, verified on disk) — 4 files / 19 tests green: sandbox string rules 7, wrapper stage order 7 (schema rejection short-circuits with an empty stage log; `..` escape refused with `{error:'sandbox'}` and zero logs; new-file risk-1 runs silent with `order == ['risk','snapshot']` proving snapshot-before-execute; existing-file risk-2 blocks on the approval hook (`order == ['risk','approval','snapshot']`) and the snapshot captures the OLD content; skip → no snapshot/no mutation; cancel → same with cancel status; unknown tool refused), workspace-fs containment backstop 3, result truncation 2 (8 KB cap — `MAX_TOOL_OUTPUT_BYTES`); plus write_file atomic write through the injected facade with zero `.agento-tmp` leftovers. Gate in BOTH shells: PowerShell `npm run typecheck` 0 / `npm run lint` 0 / `npm test` 19 passed; Git Bash identical (exit 0 ×3); `uv run pytest` 3 passed in both. App gates unchanged otherwise — the agent tree is additive; no runtime code path changed yet. Report `phase-reports/M2.1-tool-registry-wrapper.md`.)*

### M2.2 — Workspace picker
- [x] Workspace picker (native folder dialog), recents, workspace context attached to sessions

**Done when:** a picked workspace sticks across restarts (recents) and attaches to new sessions. *(Met: vitest `workspaces.test.ts` 7/7 — recents round-trip across re-init (the persisted workspaces.json is what survives), cap 10 most-recent-first with case-insensitive de-dup, corrupt-file tolerance + recovery, zero atomic-write temp leftovers, missing-folder plain-language refusal; plus the Electron-ABI harness `M22_ATTACH_OK` (esbuild-bundled repos run inside a real Electron main, temp userData + temp DB through the real drizzle migrations): pick → persist → canonical realpath; `session:create`'s exact composition (`workspacePath: getCurrentWorkspace() ?? undefined`) stores the picked workspace in the sessions row; fresh-install init keeps the `''` placeholder (docs/03 §8 NOT NULL, M1.3 convention); re-init (restart) restores the pick and the next session stamps it again. Live UI: CDP probe `M22_CDP_OK` 6/6 — chip renders in the sidebar with the honest "No workspace selected" empty state, click opens the recents menu with "Choose folder…", `workspace:get` resolves `{current, recents}` over the typed bridge, `session:list` rows carry `workspacePath` (4 rows), zero renderer console errors; the native dialog itself is not CDP-automatable — its glue is 6 lines in `ipc/workspaces.ts` delegating to the tested store. Gate in BOTH shells: PowerShell + Git Bash typecheck 0 / lint 0 / vitest 26 passed (5 files) / pytest 3 passed. Report `phase-reports/M2.2-workspace-picker.md`.)*

### M2.3 — Sandbox guard
- [x] Workspace sandbox guard (`docs/06-security-and-permissions.md` §4) + adversarial fixture tests passing

**Done when:** the adversarial fixture suite passes (release-blocking). *(Met: hardened `sandbox.ts` to the full docs/06 §4 form — lexical containment (both separators) → realpath walk over every EXISTING component (probed with lstatSync, NOT existsSync, so junction loops aren't hidden) → mode policy (reads may follow an outside-pointing link, writes refuse; once a read has traversed outside, the rest of the path stays permitted) → ELOOP → plain-language refusal, never a hang → protected-name deny list (id_rsa/id_dsa/id_ecdsa/id_ed25519/.ssh/.gnupg/.aws/.netrc/credentials, case-insensitive, with single-extension variants like credentials.json, overridable per-path). `sandbox.fixtures.test.ts` 39 passed + 2 skipped (true-symlink cases feature-detected: this box can't create symlinks without Developer Mode, so the symlink-escape and symlink-loop cases are skipped honestly — the junction-escape, junction-LOOP (two cross-pointing junctions reproduce ELOOP without symlink privileges), Unicode, long/deep, case-insensitivity, protected-name, and wrapper-integration cases all run unconditionally). Plus the M2.1 string-rule suite updated to a real temp root (7). Gate in BOTH shells: PowerShell + Git Bash typecheck 0 / lint 0 / vitest 39 passed + 2 skipped (6 files) / pytest 3 passed. Report `phase-reports/M2.3-sandbox-guard.md`.)*

### M2.4 — Read-only file tools
- [x] P0 read-only tools: `list_dir`, `read_file`, `search_files`
- [x] `ask_user` (docs/03 §5): blocks the loop on the user's reply in the thread — powers the UNDERSTAND step of the system prompt
- [x] Tool cards render for these tools (assistant-ui tool UIs) with plain-language titles

**Done when:** the three tools and `ask_user` run through the wrapper and render as plain-language cards.

### M2.5 — First mutations: create_dir + write_file
- [x] P0 mutating tools: `create_dir`, `write_file` — first run through the wrapper proves snapshotting
- [x] Tool cards render for these tools with plain-language titles

**Done when:** a create/write run lands a checkpoint that the (stub or real) Changes view can see. *(Met: harness-level proof — `create_dir.test.ts` 5/5 (risk 1 new-path `[risk,snapshot]` no-approval + existed:0 snapshot; risk 2 existing-dir 1 approval + honest no-op; file-at-path refusal; schema rejection pre-stage), `write_file.test.ts` 4/4 (`excerptOf` 8-line/600-char caps + overwrite captures OLD content with 1 pre-mutation snapshot); durable path is wired not mocked — snapshot hook → `recordCheckpoint` (migration 0003 `checkpoints`/`tool_calls`) → `changes:list` IPC (relative paths only) → `ChangesStub` refreshing on run settle; write_file after-excerpts backfilled by toolCallId; durable-layer round-trip has no plain-vitest test because better-sqlite3 is Electron-ABI (M2.2 precedent — live e2e belongs to the M2 milestone gate); typecheck + lint + vitest 81 passed + 2 skipped + pytest 3 passed.)*

### M2.6 — edit_file + before/after
- [x] `edit_file` result includes before/after excerpts, rendered in the card — no diff component anywhere

**Done when:** an edit shows before/after excerpts in its card, and no diff component exists in the repo. *(Met: `edit_file.test.ts` 8/8 — anchor exactly-once (zero/multi-match plain-language refusals, missing-file refusal, empty-anchor schema rejection), success replaces with region excerpts + pre-mutation snapshot (`['approval','snapshot']` — risk 2 unconditional, no disk probe); `EditFileCard` on the `setToolUI` slot + `changes:list` "Edited file" title + checkpoint after-excerpt backfill via the tool's own `editExcerpts`; repo-wide scan: no diff imports, no diff components, no diff deps; typecheck + lint + vitest 89 passed + 2 skipped + pytest 3 passed.)*

### M2.7 — move / copy / delete
- [x] P0 mutating tools: `move_path`, `copy_path`, `delete_path`
- [x] Tool cards render for these tools with plain-language titles

**Done when:** all three run through cards with snapshots taken. *(Met: `move_path.test.ts` 7/7 (always risk 2, `['approval','snapshot','snapshot']`, source row carries `dest_path`, onto-existing replaces with old-content snapshot, same-path no-op, folder rename), `copy_path.test.ts` 6/6 (risk 1 new / 2 onto-existing mirroring write_file, byte-exact copy, folder-source refusal), `delete_path.test.ts` 6/6 (always risk 3, content-holding snapshot, non-empty-dir + missing-path honest refusals); facade `movePath`/`copyPath`/`deletePath` with containment backstop, no `fs` in tool bodies; MovePathCard/CopyPathCard/DeletePathCard on the `setToolUI` slots + `changes:list` titles; docs/04 §4 logged same change; typecheck + lint + vitest 108 passed + 2 skipped in BOTH shells, pytest 3 passed in both.)*

### M2.8 — Changes panel + undo
- [x] Changes panel: checkpoints listed, per-item undo, Undo all (undo matrix tests green)

**Done when:** undo-matrix tests pass; per-item undo and Undo all restore prior state. *(Met: `undo.test.ts` 14/14 — create/edit/move/delete/dir restores, move-pair group replay (source row renames back before the dest row restores — newest-first would destroy the moved bytes), move-onto-existing both-sides restore, sha-mismatch + evicted refusals (no stray rows, rows stay retryable), undo-of-undo, undo-all continues past refusals, undo rows are solo groups (null toolCallId — the "restored ✓" fix); engine storage-free in `agent/undo.ts` with an in-memory store + real temp WorkspaceFs (better-sqlite3 can't load under vitest — M2.2 precedent); durable adapter = 3 thin repo fns; `changes:undo`/`changes:undo-all` IPC (blocked mid-run via chat's map) + `ChangesPanel` (grouped items, inline confirms, reverted dim + "restored ✓", honest error surfacing, `.changes-panel__list` joined to the scrollbar selectors); migration 0004 `is_dir` (eviction-proof dir marker) + rowid-ordered feed; docs/03 §4/§8 + docs/04 §4 updated same change; typecheck + lint + vitest 122 passed + 2 skipped in BOTH shells, pytest 3 passed in both.)*

**Milestone gate:** "make a copy of report.md named backup.md and add today's date to it" runs end-to-end with cards, before/after, and a working undo. *(Met: live CDP run 25/25 — copy card on attempt 1 (steering held), edit card with Before/After, backup.md holds report content + today's date, per-item undo removed the date with "restored ✓" dim, Undo all removed backup.md with report.md byte-identical, zero console errors, localhost-only egress; DB dump ALL CHECKS PASSED — 5 migrations, 6 tables, copy_path + edit_file in tool_calls, 7 checkpoint rows (4 reverted, 3 active undo rows); report `phase-reports/M2-milestone-gate.md`.)*

## M3 — The trust loop *(thesis-critical — never cut)*

*Goal: plan-first execution with approvals and verification, visible everywhere.*

### M3.1 — Plan mode + PlanPanel
- [x] Plan mode: structured plan → PlanPanel; execution starts only on user go-ahead *(1-hour slice 2026-09-09: fixed the 3 failing plan-run tests — single-terminal hold semantics + plan-less degradation runs execution; 4/4 green)*

**Done when:** a mutating request produces a plan first and executes only after the go-ahead.

### M3.2 — Rule-table risk gating
- [x] Risk classification: rule table (TS mirror) wired into the wrapper; risk ≥ 2 blocks on the ApprovalDialog promise *(1-hour slice: real pending-promise `requestApproval` + `approval:respond` IPC + blocking ApprovalDialog; `approval/requested` + `approval/resolved` join the event union both sides)*

**Done when:** a risk ≥ 2 tool call blocks on the ApprovalDialog promise.

### M3.3 — Approval coalescing
- [x] Approval coalescing: consecutive same-shape calls inside one plan step ask once (e.g. moving 42 files = 1 approval showing the count) *(1-hour slice STUB: same-tool grouping, 1 request for 42, count shown; 25% over-projection re-ask + bulk >25 → risk 3 DEFERRED — see Devlog)*

**Done when:** 42 same-shape calls ask once, with the count shown.

### M3.4 — Deny & cancel semantics
- [x] Deny → step skipped cleanly, run continues; Cancel → stops after current step *(1-hour slice: cancel flag consumed by `stopWhen` at step boundaries; skip = SDK continues; unit-covered via approval promise fan-out)*

**Done when:** deny skips the step and the run continues; cancel stops after the current step.

### M3.5 — Verification loop
- [x] Verification loop: `/completion/verify` per mutating step; verified ✓ badge; one retry with missed segments; honest "not verified" when service absent *(1-hour slice: `verifyStep` client built exactly to the docs/05 §2 contract with 4 fake-fetch tests; loop does one retry on missed segments; degraded default injected as skipped → honest "not verified"; real endpoint lands in M4.3)*

**Done when:** mutating steps verify; the ✓ badge shows, one retry fires on missed segments, and a missing service yields an honest "not verified".

### M3.6 — PlanPanel live statuses
- [x] PlanPanel live statuses incl. verification badges; failed steps show missed requirements *(1-hour slice: live `statuses`/`verification`/`errors` state routed from `plan/step_updated` + `verification/finished`; footer N-of-M; no CDP probe — deferred)*

**Done when:** the PlanPanel updates live (badges included) and failed steps show their missed requirements.

### M3.7 — Degraded-mode dress rehearsal
- [ ] Dress rehearsal (manual): a **file-organization variant** of the demo scenario — no document tools, which don't exist yet and need Python — runs in degraded mode: plan → 1 batch approval → execute → verify (honest "not verified") → undo-all works *(OPEN: not run in the 1-hour slice)*
- [x] Scripted e2e: a fake provider emitting a deterministic tool-call script (docs/07 §2) drives plan → approval → execute → verify → undo, incl. cancel mid-run and MAX_STEPS *(1-hour slice SMOKE: happy-path `plan-run.e2e.test.ts` green — plan → approve → execute → verify-skipped; deny/cancel/MAX_STEPS matrix DEFERRED)*

**Done when:** the variant passes end-to-end with the sidecar not running, and the scripted e2e is green.

**Milestone gate:** a file-organization variant of the demo scenario runs in degraded mode (no Python): plan → 1 batch approval → execute → verify → undo-all works.

## M4 — Intelligence sidecar (Python) *(thesis-critical)*

*Goal: the trust layer and document engine come alive.*

*Parallel-work note (docs/08 §4): the endpoint/eval sub-phases are Python-track work buildable without touching `app/`; the "wired into the loop" clause in M4.1, the TS half of M4.2's parity tests, and the banner/reconnect work in M4.5 are JS-track. One agent doing M4 in order covers both halves.*

### M4.1 — /intent/classify
- [ ] `POST /intent/classify` (LLM-prompted v1, structured output) live per docs/05 §2, contract tests green
- [ ] Wired into the loop: main's intelligence client calls it once per instruction; `requires_clarification` biases the model toward `ask_user` (docs/05 §2)

**Done when:** the endpoint answers per contract and the loop consumes its structured output.

### M4.2 — /safety/classify + parity
- [ ] `POST /safety/classify` (rule table primary + LLM fallback, floor semantics) — TS/Python rule tables pass the same fixture tests

**Done when:** the TS and Python rule tables pass the same fixture tests.

### M4.3 — /completion/verify
- [ ] `POST /completion/verify` (LLM judge) replaces nothing, backs M3's loop when present

**Done when:** M3's verification loop uses the judge whenever the sidecar is up.

### M4.4 — Token enforcement proven
- [ ] Every endpoint (not just `/health`) refuses tokenless requests — contract tests green (the auth itself ships with the sidecar since M0.4/M0.5)

**Done when:** a tokenless request to each endpoint returns 403 in the test suite.

### M4.5 — Degraded mode
- [ ] Degraded mode: kill the sidecar mid-session → banner shows, file tools keep working, no crash; restart reconnects

**Done when:** killing the sidecar mid-session shows the banner, tools keep working, and a restart reconnects.

### M4.6 — Eval harness skeleton
- [ ] Eval harness skeleton: `uv run eval` produces a first metrics table from seed data (`docs/05-intelligence-service.md` §5)

**Done when:** `uv run eval` prints a real metrics table from seed data.

### M4.7 — Docling document endpoints
- [ ] Docling endpoints live per docs/05 §2: `POST /documents/parse`, `POST /documents/edit` (python-docx round-trip, styles preserved), `POST /documents/convert` (`docx`/`csv`/`md` targets)
- [ ] Parse-fixture tests green: a small PDF with a known table and known headings asserts the markdown structure (docs/05 §6)

**Done when:** the fixtures parse to the expected structure and a `.docx` edit round-trip preserves styling.

**Milestone gate:** killing Python mid-run degrades gracefully and `/eval` outputs a real table.

## M5 — Documents, web & batch automation

*Goal: the general-agent payload.*

### M5.1 — read_document on real files
- [ ] `read_document` tool (schema per docs/03 §5 tool inventory) wired to `POST /documents/parse` (built in M4.7) against real `.pdf/.docx/.pptx/.xlsx` files

**Done when:** a real 40-page PDF — plus a `.docx`, `.pptx`, and `.xlsx` — parses through the tool.

### M5.2 — summarize_document
- [ ] `summarize_document` tool (schema per docs/03 §5 tool inventory) — summaries save as `.md` files

**Done when:** a summary lands as a saved `.md` file.

### M5.3 — edit_document
- [ ] `edit_document`: `.md/.txt` direct; `.docx` via `POST /documents/edit` round-trip with before/after excerpts

**Done when:** a `.docx` edit round-trips with before/after excerpts.

### M5.4 — convert_document
- [ ] `convert_document`: md→docx, `.docx`→md, md/html→pdf (Electron `printToPDF`), xlsx→csv; honest unsupported-format errors

**Done when:** each conversion produces an openable output and unsupported formats fail honestly.

### M5.5 — Document preview *(P1 — cuttable)*
- [ ] Document preview (right panel: pdf.js / DOCX→HTML / markdown / text)

**Done when:** each supported type previews in the right panel.

### M5.6 — web_fetch / web_search
- [ ] `web_fetch` (+ `web_search` if a search key is configured) with untrusted-content framing

**Done when:** fetched content enters the conversation framed as untrusted.

### M5.7 — Batch flow end-to-end
- [ ] Batch flows proven: "organize downloads by type, then summarize every PDF" end-to-end

**Done when:** the batch scenario completes in one run with one batch approval.

**Milestone gate:** a real 40-page PDF is parsed, summarized to a saved `.md`, and a messy folder is reorganized with one approval — all visible in cards and preview.

## M6 — Polish, packaging & numbers

*Goal: production shape and defensible results.*

### M6.1 — Onboarding + empty states
- [ ] Onboarding (welcome → workspace → key), empty states

**Done when:** a fresh launch walks through welcome → workspace → key with sensible empty states.

### M6.2 — QuickActions *(P1 — cuttable)*
- [ ] Quick actions chips

**Done when:** a chip starts a common flow in one click.

### M6.3 — Settings completion
- [ ] Settings completion: permission defaults per tier (risk 3 locked to always-ask), theme, **Data** section (open data folder, clear sessions, purge snapshots, eval-data export — docs/04 §3.7; the export feeds docs/05 §5)

**Done when:** tier defaults are enforced (risk 3 always asks), the theme toggles, and the Data actions work — incl. eval export.

### M6.4 — MCP client *(P1 — cuttable)*
- [ ] MCP client: user-added servers' tools flow through the registry (risk floor 1); MCP server config in settings

**Done when:** a user-added server's tools run through the registry at risk floor 1.

### M6.5 — Ollama *(P1 — cuttable)*
- [ ] Local models via Ollama working (P1)

**Done when:** a local Ollama model chats and calls tools.

### M6.6 — NSIS packaging
- [ ] electron-builder **NSIS build installed and working on a clean Windows machine**

**Done when:** the installer works on a clean Windows machine.

### M6.7 — Eval dataset + final numbers
- [ ] Eval dataset at target sizes (`docs/05` §5); final metric tables regenerate from one command

**Done when:** the final tables regenerate from one command.

### M6.8 — Full manual checklist
- [ ] Full manual checklist (`docs/07-testing-and-demo.md` §2) passes

**Done when:** the checklist passes.

**Milestone gate:** a non-developer can install and use it with only an API key, and the thesis numbers regenerate reproducibly.

---

## One-day MVP — shipped 2026-09-10 (per `MVP_PLAN.md`)

**Shipped** (each step committed + gated; `docs/05` "MVP cut" section + STACK.md rows in the same commits):

| Item | State |
|---|---|
| `read_document` tool | ✅ risk 0; pypdf/python-docx via sidecar `/document/extract` for `.pdf/.docx`, direct read for text (sidecar-down-safe), 6k-char honest cap |
| `summarize_document` tool | ✅ risk 0; extract + one-shot LLM completion over the user's provider TS-side (`ctx.llm`) — no LLM or key ever reaches Python |
| `web_fetch` tool | ✅ risk 0; plain GET (10 s / 2 MB caps), dependency-free HTML→text + `<title>` |
| `semantic_search` tool (standout) | ✅ risk 0; fastembed `all-MiniLM-L6-v2` on-device via sidecar `/embed/embed`, content-hash-keyed JSON cache in userData, cosine top-k, ranked card with click-to-open (new sandbox-contained `system:open-path` IPC); boot-time model warmup |
| `/intent/classify` + `/safety/classify` | ✅ heuristic-only (honest `confidence: "heuristic"`), wired **audit-only** — the trust loop is untouched |
| `/completion/verify` | ✅ heuristic postconditions matching the SHIPPED TS client's flattened contract; M3.5's skipped-stub replaced; read-only runs skip honestly |
| Onboarding | ✅ one static screen (workspace pick + provider/key), App-level gate, no new IPC |
| Demo path | ✅ `electron-vite dev` — boot smoke verified (clean build, DB migrated, sidecar healthy on 7891, zero log errors, clean teardown) |

**Explicitly deferred (unchanged from MVP_PLAN):** Docling integration, `edit_document`/`convert_document`, `web_search`, batch flows, eval harness, NSIS/auto-update, multi-step onboarding. Plus MVP-cut deviations: `/document/summarize` endpoint dropped (summarization runs TS-side — keys stay in main); embeddings cache in userData instead of `.agento/` in the workspace (a risk-0 tool must not write there); classifier LLM-fallback not built (heuristic-only, shapes leave room).

**Open for the user (needs a real API key):** the interactive demo run-through (summarize a doc → fetch a URL → "where did I write about pricing?" → approval dialog → verification badge → undo). The automated gates and boot smoke are green; the live model exchange is the one thing automation can't vouch for.

### UI polish — real three-column shell *(`UI_POLISH_PLAN.md`, 2026-09-10)*
- [x] Overlap fixed by construction: `main.chat-main` (flex:1) owns the thread; Plan + Changes are in-flow children of one `div.right-rail` — no `position: fixed` panels, no 45vh offset hack (`.changes-panel--plan-open` deleted)
- [x] Settings + sidecar status dot moved into a hairline-bordered sidebar footer (was floating top-right chrome)
- [x] Rail collapses when neither a plan nor an active session exists
- [x] Light sidebar polish only: bordered full-width `+ New chat`, tightened session rows, "Conversations" label; `.thread` max-width 56→48rem (spec §7) — no color/token changes, no new deps
- [x] docs/04 §2/§4 updated (hard rule 6); typecheck 0, lint 0, vitest 32 files / 196 passed + 2 skipped

---

## Devlog

*(append-only; one line per entry: `YYYY-MM-DD — what/why — who`)*

- 2026-09-11 — **Out-of-order contrast pass (user-requested, ahead of M4).** Grayscale-only tiers in `main.css`: secondary text solid `muted-foreground` (70% mixes removed), placeholder washed to 55%, active conversation row variant C (12% fill + brighter border + bold title, tag flattened on active row only), Plan/Act active pill filled near-white; `App.tsx` hint previews the hovered/focused tab's description. Send button untouched. Previewed in Temp HTML first (user picked C over bar variants), then ported. Gates: typecheck 0, lint 0, vitest 40 files / 272 passed + 2 skipped. Docs/04 components log updated same change — Muse Spark

- 2026-09-11 — **Out-of-order composer fix (user-requested, ahead of M4).** The paperclip sat alone on its own row because `.file-attach` carried `flex-basis: 100%`. `FileAttach.tsx` now returns a fragment — chips (`flex-basis: 100%`) above, an inline `.attach-anchor` (paperclip · input · Send) below; popover re-anchored to the button with a fixed 22rem width. Gates: typecheck 0, lint 0, vitest 41 files / 278 passed + 2 skipped — Muse Spark

- 2026-09-10 — **Out-of-order M6.3 Settings completion + custom providers (user-requested, ahead of M4).** Settings v2 (`settings-profiles.ts` pure + `settings.ts` Electron): appearance (dark/light/system, `<html>.dark` switch), permission defaults (risk1 auto/ask, risk2 ask/auto, risk3 hard-locked) wired via `ctx.approvalPolicy` in the registry, Data (open folder, clear conversations, purge snapshots, redacted eval export via `storage/maintenance.ts`), and custom OpenAI-compatible profiles (`custom:<uuid>`, canonical base URL — https or loopback-http only, key optional, free-form model, main-only Test connection). `providers.ts` owns client construction (Google/Groq unchanged); chat allows keyless customs; keys stay in `secrets.bin`, never IPC/SQLite/sidecar. UI: full SettingsDialog sections + close button, onboarding lists customs, App applies theme incl. system listener. Tests: `settings-profiles` (19), `providers` (routing/copy), `permission-policy` (risk1-ask/risk2-auto). Gates: typecheck 0, lint 0, vitest 39 files / 257 passed + 2 skipped, pytest 42 passed. Docs updated same change (03 §4/§10, 04 §3.7 + components log, 06 §7). No new deps (STACK.md untouched). Deferred per plan: snapshot-size controls, session provider-history migration — Muse Spark

- 2026-09-10 — **MVP repair slice 1: run-status indicator + plan-step live statuses + Groq file-request hardening.** User report: only three dots while waiting, and "make a txt file" silently stopped (`tool_use_failed` in the dev log). Fixes: (1) thread-level `.run-status` phase line (preparing / plan ready / waiting for approval / working / verifying, `aria-live`, reduced-motion safe) fed by `chat.status` + existing `agent:event`s — no new channel; (2) `chat.ts` audit sink now maps every tool outcome to `plan/step_updated` (done/failed/skipped) so PlanPanel rows leave pending; (3) `plan-run.ts`: text-only plan answers to likely-file-changing requests fail loudly with the plan copy instead of delivering a dead "Sure!"; execution retries once with `toolChoice: 'required'` when an approved plan yields zero tool calls, then a zero-action guard ends loudly instead of a false success; usage sums across the retry. Tests: new `plan-run.write-file.test.ts` (plan → approve → real `write_file` lands `notes.txt` on a temp workspace; text-only file request errors; heuristic unit checks) + M3.8 doubling test now uses a greeting prompt. Gates: typecheck 0, lint 0, vitest 34 files / 209 passed + 2 skipped, pytest 40 passed — Muse Spark
- 2026-09-11 — **M3.8: plan-phase failure semantics + diagnosability (the "hey" → "check your connection" lie).** Live trace: Groq `openai/gpt-oss-120b` answering a greeting produced a forced-call `tool_use_failed` 400 (attempt 1) then a text-only `auto` retry (attempt 2) — and the loop replaced that legitimate reply with attempt 1's held error, classified as the generic "Check your connection" copy. Four root causes fixed in `plan-run.ts`: (1) **diagnosability** — `friendlyProviderError` now logs `[provider] <status>: <message> body=<responseBody>` via `console.error` before classifying; response headers/request body are never logged (headers carry the key; `requestBodyValues` may echo conversation content); (2) **failure semantics** — plan-phase parts are now HELD per attempt and only the ONE attempt that resolves the run is committed (forwarded + accumulated), so both the thread bubble and the persisted reply are single — no doubled greeting; after both attempts, a naturally completed text-only attempt (no error part, natural finish) is delivered as a normal reply and persisted via the normal settle point, with NO execution (plan-first guarantee intact: nothing can run without a plan); (3) **copy classification** — non-key 400s in the plan phase (the `tool_use_failed` class) now say "I couldn't create a plan for that. Try rephrasing the request." instead of lying about the connection; execution-phase 400s keep the generic copy; 401/403 + 400-with-"api key" + 429 still classify first and RetryError is unwrapped before classifying. `friendlyProviderError` exported for tests; docs/04 §5 copy table gained the plan-phase row. Gates: typecheck 0, lint 0, vitest 32 files / **203 passed** + 2 skipped (7 new: forced-refusal-then-text-reply delivered as a normal reply, text-only-retry never doubled, classifier matrix incl. the console log), pytest untouched — Cline
- 2026-09-01 — Spec baseline written (agent-first docs rev 2; assistant-ui + AI SDK + MCP + Docling strategy) — ZCode
- 2026-09-01 — Spec review pass: `edit_file` anchor schema, built-in tool inventory incl. `ask_user`/`emit_plan`, approval-coalescing mechanism specified (docs/03 §5); `checkpoints.dest_path` for move-undo + honest refusal on evicted-snapshot undo (docs/03 §7–8, docs/01 §4); pdfjs-dist + mammoth added to STACK.md; Electron-ABI + coalescing risks added (docs/08 §7); search keys in secrets map (docs/06 §7); roadmap added to AGENTS.md doc map — ZCode
- 2026-09-01 — assistant-ui/Docling de-risking: M0 thread uses the real IPC transport with a canned echo stream + `ai`/assistant-ui/bridge trio pinning (PROGRESS M0, docs/02 §2.1); Docling cold-start spike (week-1/2, timings to Devlog) + demo pre-warm step (docs/07 §4); honest parse-failure behavior + human-gated parser-substitution contingency + parse-fixture tests (docs/05 §6); risks added to docs/08 §7 — ZCode
- 2026-09-01 — Node.js runtime moved from 20 LTS to **24 LTS** (20 is EOL; 24.20.0 is the current LTS): STACK.md + docs/02 §1 updated per rule 1. nvm-windows 2.0-rc abandoned — its hard-coded 3-second manifest-fetch budget cannot complete on slow links and the shim sat behind the machine-PATH Node install; Node 24.20.0 installed directly from the official zip (SHA256 verified against SHASUMS256.txt) — ZCode
- 2026-09-01 — better-sqlite3 ABI risk fully specified: M0 DB gate checkbox (smoke test; `@electron/rebuild` only on mismatch; Electron exact-pinned; Devlog records prebuild vs rebuild) in PROGRESS M0 + docs/08 §7; human-gated `node:sqlite` substitution contingency added to docs/03 §8 — ZCode
- 2026-09-02 — Plan restructured into **48 half-day sub-phases (M0.1–M6.8)** under unchanged M0–M6 milestones (docs/08 §3 tables + per-sub-phase sections here): every prior acceptance criterion preserved, only regrouped — P0 file tools split across M2.4–M2.7 with the tool-cards criterion folded into each tool phase; "quick actions, onboarding, empty states" split into M6.1/M6.2; MCP-server-config setting moved from the M6 settings box into M6.4; one order change: workspace picker now M2.2, before any file tool, so the sandbox and tool demos have a real workspace root from day one — ZCode
- 2026-09-02 — Pre-implementation review pass over the full spec set; fixes: M3.7 dress rehearsal now a **file-organization variant** of the demo scenario (the M5 scenario needs Docling — impossible degraded/pre-M5) + scripted fake-provider e2e added (docs/07 §2); Docling `/documents/*` given a home as **M4.7** (parse/edit/convert + parse fixtures) so M5.1–M5.4 are tool wiring (now 49 sub-phases); per-launch token auth moved into the sidecar lifecycle (M0.4 enforces 403, M0.5 generates + sends the token; M4.4 = enforcement tests) per docs/05 §1 / docs/02 §2.4; docs/08 §4 states the Python/JS halves of M4 explicitly; Settings→Data section incl. eval export added to M6.3 (docs/04 §3.7, docs/05 §5); `ask_user` scheduled in M2.4; `.docx`→md conversion added to M5.4 (docs/02 §2.6); M1.3 `workspace_path` placeholder convention noted (NOT NULL before the M2.2 picker); M5.5/M6.2 marked *(P1 — cuttable)* per the §6 cut order; docs/05 §6 pins degraded document-tool behavior (registered, honest offline error); docs/03 §5 example tool corrected to `write_file` (`edit_file` is always risk 2 per the rule table); README doc map updated — ZCode
- 2026-09-02 — **M0.1 done.** Scaffolded `app/` with the official `@quick-start/create-electron@1.0.30` (`--template react-ts --skip`), then stripped demo content (counter/`Versions` component, `ping` example API, demo assets). Resolved versions (from `app/package-lock.json`): electron **39.2.6** (pinned exact, no `^`/`~`), electron-vite **5.0.0**, vite **7.3.6**, react/react-dom **19.2.8**, typescript **5.9.3**, eslint **9.39.5**, electron-builder **26.15.3**. Notable: (1) template's React 19 conflicts with STACK.md's "18.x" row — updated STACK.md per AGENTS.md rule 1 instead of downgrading (assistant-ui/`ai` v5 support React 19; lockfile is the version source of truth); (2) removed the template's runtime deps `@electron-toolkit/preload` and `@electron-toolkit/utils` after the demo strip (not in STACK.md; main/preload are now plain Electron code — its tsconfig/eslint config siblings remain as devDeps); (3) `sandbox: true` works with the minimal preload (contextBridge + ipcRenderer only) — template default `sandbox: false` was not needed; (4) preload exposes a minimal typed `window.agento` bridge (`agento:ping` → `'pong'` proof-of-wiring; channel naming per docs/02 §2.1); (5) electron-builder metadata set to `appId com.agento.app` / `productName Agento` (docs/02 §5); (6) `npm run dev` verified live on Windows (PowerShell-spawned, shell-agnostic scripts): main+preload built, renderer booted, `ELECTRON_ENABLE_LOGGING=1` shows no renderer console errors, `npm run typecheck` and `npm run lint` green — ZCode
- 2026-09-02 — Housekeeping: `.gitignore` gained `phase-reports/` (local agent working notes/scratch drivers). The reported `.gitattributes` CRLF-only modification self-resolved — under `* text=auto eol=lf` + `core.autocrlf=true` git normalizes it to the identical blob, so there was nothing to commit for it; contents untouched — ZCode
- 2026-09-02 — **M0.2 done.** Renderer now renders an assistant-ui `Thread` on a hello-world local runtime: `useLocalRuntime` + a renderer-side `ChatModelAdapter` that streams a fixed echo/greeting word-by-word via cumulative `content` yields (no IPC, no provider, no network; abort-aware); Thread composed from primitives (`ThreadPrimitive.Root/Viewport/If/Messages/ViewportFooter`, `MessagePrimitive.Root/Parts`, `ComposerPrimitive.Root/Input/Send` — `Send` auto-disables while empty/running), styled minimally via existing `main.css`/`base.css` only (no tailwind/shadcn). Resolved trio pinned exact in the lockfile (from `app/package-lock.json`): `ai` **5.0.250**, `@assistant-ui/react` **0.11.56**, `@assistant-ui/react-ai-sdk` **1.1.21** (+ `@ai-sdk/react` **2.0.253**, the bridge's ai-v5 line). Notable: (1) **`ai` v7 exists and the newest bridge no longer supports v5** — `@assistant-ui/react-ai-sdk@1.2.0+` → ai v6, `1.3.41+` → ai ^7, latest `1.4.8` hard-depends on `@assistant-ui/ai-sdk@0.0.3` (ai ^7.0.77, no `ai` peer left to downgrade); the last bridge on ai v5 is **1.1.21**, whose peer `@assistant-ui/react ^0.11.56` forces `@assistant-ui/react` **0.11.x** (a downgrade from latest 0.15.17 — accepted; STACK.md mandates ai v5 and overrides were banned); (2) **0.11.58 is broken with `useLocalRuntime`** — 0.11.57 introduced an unbound `__internal_setGetInitializePromise` call in `RemoteThreadListHookInstanceManager` (`Cannot set properties of undefined` inside `_InnerActiveThreadProvider`, crashes the tree); pinned 0.11.56 (still satisfies the peer range, and is what the bridge line was developed against); (3) docs surprise: assistant-ui's current docs dropped the LocalRuntime guide and now document the v7-era `@assistant-ui/ai-sdk` package instead — APIs verified against the published 0.11.56 `.d.ts` and the current Model Adapters API reference instead; `ChatModelRunOptions.config` was renamed to `context` (old field kept as deprecated); (4) `ThreadMessage.content` is always a part array in 0.11 (no plain-string variant); (5) hello-world exchange verified end-to-end headlessly via CDP (send → streamed echo renders in the Thread); observed-only: renderer `setTimeout` chunks throttle when the window is occluded — irrelevant for M0.3, whose stream arrives over IPC events; `agento:ping` pair left untouched per plan — ZCode
- 2026-09-03 — **M0.3 done.** The Thread now runs on the real custom IPC transport: new `src/main/ipc/chat.ts` registers `chat:send` and emits a canned ai-v5 `UIMessageChunk` sequence (`start` → `start-step` → `text-start` → `text-delta`×N echoing the user's message word-by-word → `text-end` → `finish-step` → `finish`, exact member shapes verified in ai 5.0.250's `.d.ts`), each part forwarded verbatim as `sender.send('chat:part', { sessionId, part })` with 50 ms pacing (main-process timers, so no occlusion throttling); `agento:ping` removed. Preload bridge replaced by a typed `window.agento.chat` surface: `send({ messages })` (invokes `chat:send`) + `onPart(listener)` returning an unsubscribe fn — renderer never touches `ipcRenderer`. Renderer swapped `useLocalRuntime`/hello-world adapter for `useChat({ transport })` (`@ai-sdk/react`) + `useAISDKRuntime(chat)` (`@assistant-ui/react-ai-sdk`) over the new `chat/transport.ts` IPC `ChatTransport<UIMessage>`; **verbatim guarantee:** `sendMessages` subscribes, invokes, and enqueues every received `part` object into a `ReadableStream<UIMessageChunk>` untouched (filtering by envelope sessionId only) — Electron's structured clone is the only transformation between main and `useChat`, so the Thread renders exactly what main emitted; `reconnectToStream` → `null`. Verified API shapes (installed `.d.ts`/`.js`, not the v7-era website): `ChatTransport`/`UIMessage`/`UIMessageChunk` export from **`ai`** (`@ai-sdk/react` re-exports only `CreateUIMessage`/`UIMessage`/`UseCompletionOptions`); `useChat<UI_MESSAGE>({ transport?, ... }?: UseChatOptions)`, `UseChatOptions = ({ chat } | ChatInit) & { experimental_throttle?, resume? }`; 1.1.21 exports `useChatRuntime`, `useAISDKRuntime`, `AssistantChatTransport` — **`useChatRuntime` takes `ChatInit` options and spins its own `useChat` under `unstable_useRemoteThreadListRuntime`; it does not accept a `chat` param** (the docs/02 §2.1 sketch's `useChatRuntime({ chat })` doesn't exist), so `useAISDKRuntime(chatHelpers)` is the hook matching our wiring. Deviations, docs/02 §2.1 updated same commit per rule 6: invoke payload is `{ messages }` (renderer sends no sessionId — main owns the `'session-placeholder'` constant until M1.3 and stamps it on `chat:part` envelopes); welcome copy de-hello-worlded (only non-CSS/copy Thread change). Abort not wired end-to-end (M1.4; no stop button in the composition, so no reachable abort path; transport ignores `abortSignal` this phase). Dev-run gate (headless CDP, `phase-reports/cdp-driver-m03.mjs`, gitignored): sent "Hello Agento" → 25 distinct assistant renders observed (one per delta), every intermediate a strict prefix of the final text, final text byte-identical to main's canned reply, zero renderer console errors; `npm run typecheck && npm run lint` green. M0.4: nothing further needed from this wire — `chat:send` stays canned until M1.2 swaps the generator for `streamText`; don't bump `@assistant-ui/react` past 0.11.56 (M0.2 note still applies) — ZCode
- 2026-09-03 — **M0.4 done.** New uv project `services/intelligence/` (Python 3.12 via committed `.python-version`; uv picked the local CPython 3.12.3, no download): FastAPI app `agento_intelligence.main:app` with one endpoint, `GET /health`, returning exactly the docs/05 §2 schema with honest capability values (`docling`/`llm_classifiers`/`trained_models` all `false` this phase); `version` ("0.1.0") is read from `pyproject.toml` at import via stdlib `tomllib` (single source of truth — the project is unpackaged, so `importlib.metadata` can't serve it). **Token auth ships with the sidecar** as app-level pure-ASGI middleware (`agento_intelligence/auth.py` via `add_middleware`, so every future route inherits it): every request must carry `X-Agento-Token` matching env var **`AGENTO_INTELLIGENCE_TOKEN`** (read once at startup); missing/wrong → 403 `{"detail":"Forbidden: a valid X-Agento-Token header is required."}`; **env unset → fail closed** — every request 403s and a startup warning is logged (chose this over boot-refusal so a misconfigured spawn is observable via /health probing); compare is constant-time (`secrets.compare_digest` on UTF-8 bytes). Resolved versions from `uv.lock`: fastapi **0.141.1**, uvicorn **0.52.4**, pydantic **2.13.5** — 14 packages total, i.e. only the three allowed runtime deps + their transitive closure, because the project is intentionally **unpackaged** (`[tool.uv] package = false`, no build backend — STACK.md lists none, and a hatchling-style backend would put a non-STACK package in the lockfile). **What M0.5 must know for the spawn wiring:** spawn exactly `uv run uvicorn agento_intelligence.main:app --port 7891` with **cwd = `services/intelligence/`** — cwd matters, since uvicorn puts it on `sys.path` and that is how `agento_intelligence` resolves (there is no installed package); set the per-launch random token in the child's env as `AGENTO_INTELLIGENCE_TOKEN` before start; the server binds 127.0.0.1 (uvicorn's default host — only `--port` is passed, matching docs/02 §2.4), verified via `netstat` (only `127.0.0.1:7891 LISTENING`); `/health` answers immediately (no heavy imports yet). Verified the full gate from BOTH shells — Git Bash (`phase-reports/verify-m04.sh`, curl) and PowerShell (`phase-reports/verify-m04.ps1`, Invoke-WebRequest, PS 5.1-compatible): with token → 200 + exact body; tokenless → 403; wrong token → 403; plus a fail-closed run without the env var (warning in output, old token now 403s). `app/` untouched — `npm run typecheck && npm run lint` green. Minor notes: uv warned "Failed to hardlink … falling back to full copy" (uv cache on C:, repo on D:) — cosmetic; set `UV_LINK_MODE=copy` to silence. Quirk worth remembering: this machine's bash 5.3 (cygwin) mangles assignments when the `${VAR:?message}` string contains an apostrophe ("server's") — the verify script's message is apostrophe-free now — ZCode
- 2026-09-03 — **M0.5 done.** New Electron-free `src/main/sidecar.ts` (plain Node TS, AGENTS.md rule 1) owns the sidecar lifecycle per docs/02 §2.4: `generateSidecarToken()` = `randomBytes(32).toString('hex')`, called once per launch in `whenReady`; `startSidecar(token, cwd)` spawns exactly `uv run uvicorn agento_intelligence.main:app --port 7891` with cwd `resolve(app.getAppPath(), '..', 'services', 'intelligence')` (dev layout — packaged equivalent is M6.6), child env carries `AGENTO_INTELLIGENCE_TOKEN` (+ `UV_LINK_MODE=copy` per the M0.4 note, keeps captured stderr clean), `windowsHide: true`, stderr piped into an 8-line ring buffer for diagnostics. Health polling: 500 ms boot poll with a 15 s ceiling and a 2 s per-request abort; first 200 `{"status":"ok"}` → healthy; spawn error / child exit / ceiling → unhealthy; after healthy (or unhealthy) the poll drops to a light 5 s cadence so the dot stays honest — no auto-restart (M4.5 owns restart/reconnect), and `/health` is polled through the token helper because the M0.4 middleware 403s it too. `sidecarFetch(path, init)` is the ONE fetch helper attaching `X-Agento-Token` — all future M4.x sidecar calls must go through it. New app-level status contract documented in docs/03 §4 (same commit, rule 6): `sidecar:status` push `{ status: 'starting' | 'healthy' | 'unhealthy', detail? }` on transitions only + `sidecar:get-status` invoke for late-mounting renderers — deliberately **not** a session-scoped `agent:event` (no sessionId/runId exists; the sidecar outlives sessions), so the Zod-both-sides rule (which scopes `agent:event` payloads) doesn't apply and renderer-side zod was not established; the dot is plain `useState`/`useEffect` — zustand arrives with the first custom panel (M3). Preload gained `window.agento.sidecar = { getStatus, onStatus }` (same unsubscribe shape as `chat.onPart`, types triplicated per the M0.3 convention); renderer gained `components/SidecarStatusDot.tsx` (fixed top-right dot in `main.css`, gray/green/red, 160 ms transition, tooltip = status + detail). Deviations worth knowing: transitions are pushed to **all windows**, not the focused one (`getFocusedWindow()` is null when blurred — a focused-only push would stall the dot); the status listener subscribes **before** `startSidecar` (M0.3's subscribe-before-invoking rule). **The quit gate caught a real bug:** the app-spawned `taskkill /pid <uv> /T /F` inherited Electron's job object (kill-on-close) and was killed mid-tree-kill when the app exited — uv died but python orphans kept port 7891, exactly the M0.5 failure mode; fix = spawn taskkill `detached: true` (CREATE_BREAKAWAY_FROM_JOB) so it outlives us; re-verified: graceful quit with a live chain leaves zero uv/uvicorn/python processes and 7891 free, relaunch binds cleanly. Gate evidence (CDP drivers `phase-reports/cdp-driver-m05.mjs` / `cdp-reload-m05.mjs`, gitignored — no screenshots, DOM-level): tokenless and wrong-token curl → 403 with the exact docs/05 body; dot green `rgb(34, 197, 94)`, tooltip "Sidecar: healthy", zero renderer console errors across three dev runs; external `taskkill /F` of the listening python → dot red in **2.1 s** (child-exit event, faster than one light poll) with the stderr tail in the tooltip; a CDP reload with the sidecar dead renders the dot **at mount** from the `get-status` pull (red + detail) — the same mount-pull that shows gray `starting` on cold boots; on this warm machine uv answers /health in ~1.5 s, as fast as renderer mount, so the dot renders green on mount and the gray phase is cold-boot-only. `npm run typecheck && npm run lint` green. **What M0.6 must know:** the DB gate is JS-track, `app/` only — add better-sqlite3 per STACK.md (no other deps), Electron 39.2.6 exact-pinned ⇒ Node 22.20 ABI: run the `select 1` + WAL smoke **under Electron main** (not plain node), record prebuild vs rebuild in the Devlog (`postinstall` already runs `electron-builder install-app-deps`, and @electron/rebuild is only added on mismatch); note `app/` still has **no test runner** (`npm test` doesn't exist yet — first TS tests will need vitest in devDeps + STACK.md per rule 1); M0.5's `sidecar:get-status`/push contract is the template for future non-session IPC — Zod lands with the first `agent:event` (M1.x) — ZCode
- 2026-09-03 — **M0.6 done.** The DB gate is green in the **prebuild world** — no `@electron/rebuild` devDep was needed, and none was added. `better-sqlite3` resolved from the lockfile at **12.11.1** (exact pin in `dependencies`; lockfile is the version source of truth). The version story matters and cost a rejection: npm's latest **13.0.3** was installed first and REJECTED — v13 dropped `prebuild-install` (it ships `prebuilds/*.node` tarball prebuilds, node-addon-api/N-API style), so the existing postinstall (`electron-builder install-app-deps` → `@electron/rebuild`) found no prebuild-install strategy and fell back to a node-gyp **source compile**, which failed hard with "Could not find any Visual Studio installation to use" — `npm install` exited 1 and would have stayed broken on every machine without VS Build Tools (full output captured in gitignored `phase-reports/m06-install.log`). Deliberately pinned the latest prebuild-install-based major, **12.11.1**: its postinstall ran `@electron/rebuild` with `buildFromSource=false`, and the Electron-ABI prebuild came from the prebuild cache — asset `better-sqlite3-v12.11.1-electron-v140-win32-x64.tar.gz` (Electron 39.2.6's NODE_MODULE_VERSION = 140), "Successfully installed prebuilt binary!", zero compilation, `build/Release/better_sqlite3.node` in place (verified again via `npx prebuild-install --runtime electron --target 39.2.6 --verbose` inside the module). New `src/main/storage/db.ts` — plain Node TS, **no Electron imports** (AGENTS.md rule 1, `sidecar.ts` style): `openDatabase(userDataDir)` is an **idempotent singleton** (module-level handle so better-sqlite3's GC finalizer can't close the DB mid-session, and the stable one-function API M1.3's repos will consume for the raw `Database.Database`), defensive `mkdirSync(recursive)`, `pragma('journal_mode = WAL')` + `pragma('synchronous = NORMAL')` at open; `runSmokeQuery(db)` = `select 1 as ok` (throws unless `{ok:1}`) **plus one empty `BEGIN IMMEDIATE; COMMIT`** — empirical finding (scratch probe `phase-reports/m06-wal-probe.mjs`, run under Electron's own runtime): a bare select never writes, so SQLite materializes `-wal`/`-shm` lazily and they would never appear on first run; the empty write txn creates both without touching schema (no tables, no drizzle, no migrations — M1.3). Wired in `src/main/index.ts` inside `whenReady` immediately after `setAppUserModelId` (docs/02 §4 step 1 — before sidecar spawn and `createWindow()`): success logs `[storage] opened <path> — journal_mode=wal, select-1 smoke ok` (journal_mode read back for evidence); failure → `console.error` + `dialog.showErrorBox('Agento', 'Agento could not open its data file at <path>. <err.message>')` + `app.quit()` + `return` (verified `killSidecar()` no-ops pre-start so the `before-quit` path is safe). Two package.json notes: (1) added top-level **`"productName": "Agento"`** — dev Electron resolved `userData` from `name` ("agento") and the DB would have landed in `%APPDATA%\agento`, not the documented **`%APPDATA%\Agento\agento.db`** (docs/02 §2.5, docs/03 §8); now dev == packaged (electron-builder.yml already `productName: Agento`); (2) added **`@types/better-sqlite3 ^9.6.0`** to devDependencies — v12 does NOT bundle types; **new package ⇒ STACK.md updated per AGENTS.md rule 1** (row beside better-sqlite3; note STACK.md/PROGRESS.md/docs/ are gitignored locals, so the pushed commit carries only package.json, package-lock.json, index.ts, db.ts). Gate evidence, two dev runs: run 1 from scratch — smoke log green, `agento.db` + `agento.db-wal` (0 B) + `agento.db-shm` (32 KB) on disk, sidecar dot unaffected (M0.5 untouched); **graceful quit checkpoints the WAL into `agento.db` and SQLite REMOVES `-wal`/`-shm` on last-connection exit** (correct SQLite behavior — the task's "files persist" holds under a forceful kill, which run 2 demonstrated: `taskkill //F` → all three files still on disk); run 2 reopened the existing db, smoke green again, zero errors in the log, and no reinstall/rebuild between runs (postinstall only runs on `npm install`) — binary ABI holds across launches. `npm run typecheck && npm run lint` green. **What M0.7 must know (green pipelines):** gate = `npm run typecheck && npm run lint` in `app/` + `uv run pytest` in `services/intelligence/`; the sidecar has **no tests yet** (no `tests/` dir — only `agento_intelligence/` with main/auth/schemas), so plain `uv run pytest` collects zero tests and **exits with code 5** ("no tests ran") — M0.7's "1 smoke test" means writing that first pytest (e.g. a /health + 403 token test reusing the M0.4 verify-script patterns) or pytest fails the pipeline; run it with cwd `services/intelligence/` (same sys.path mechanism as uvicorn). Keep better-sqlite3 at 12.11.1: every 12.x→13.x bump needs a compiler (VS Build Tools) or a postinstall rethink — do not hand-bump. `npm test` still doesn't exist in `app/` (vitest is M2.1, will need STACK.md per rule 1) — ZCode
- 2026-09-03 — **M0.6 reviewed (independent verification pass, no code changes needed).** Acceptance re-proven from a clean tree: `npm run typecheck` + `npm run lint` green; **two fresh live `npm run dev` runs** show `[storage] opened <userData>\agento.db — journal_mode=wal, select-1 smoke ok` reading `journal_mode` back from the pragma, with `-wal`/`-shm` materialized by the smoke's empty `BEGIN IMMEDIATE; COMMIT`; DB path confirmed at `%APPDATA%\Agento\agento.db` (the `productName` fix holds — was `%APPDATA%\agento` before); production `npm run build` exits 0 with `better-sqlite3` still a runtime `require` (externalized, native `build/Release/better_sqlite3.node` 1.9 MB untouched), and a tokenless `GET /health` still returns 403 while the sidecar is up. Sidecar teardown on quit re-verified with the M0.5 documented gesture (`taskkill /PID <main>`, no `/F` = click-X): electron exits, `before-quit → killSidecar` tree-kills the uv→uvicorn→python chain, port 7891 free, no strays, and the graceful close checkpoints WAL (only `agento.db` persists). Two non-blocking notes for later phases: (1) using `taskkill /IM electron.exe` (all processes) instead of the main pid orphans the sidecar chain — the documented `/PID <main>` close path is fine, but the dev-only `SIGINT`/`SIGTERM` guards in `sidecar.ts` don't cover taskkill-and-kill hooks; M2's process-model work may want a stronger teardown. (2) `npm run build` prints two benign Rollup warnings to stderr from zod 4.5.4's `@__PURE__` annotation comments (`node_modules/zod/v4/core/regexes.js`, `util.js`) — cosmetic and pre-existing (zod is the ai-trio's transitive dep), but PowerShell surfaces them as red "errors"; exit code is 0. — ZCode
- 2026-09-03 — **M0.7 done.** First pytest in the repo — the green-pipelines gate is now real. `uv add --dev pytest` resolved **pytest 9.1.1** (exact version from `uv.lock`; PEP 735 `[dependency-groups]` dev group in pyproject.toml; transitive closure adds colorama 0.4.6, iniconfig 2.3.0, packaging 26.3, pluggy 1.6.0, pygments 2.21.0 — all permissive; no other packages). Two subtleties drove the design, both documented in `conftest.py`'s docstring: (1) **import-time token** — `EXPECTED_TOKEN` is read from `AGENTO_INTELLIGENCE_TOKEN` at module level in `agento_intelligence/main.py` (fail-closed), so the root-level `services/intelligence/conftest.py` sets a fixed smoke token at its own import time, before any test module imports the app; (2) **import-path recipe: root-level conftest.py** — the pytest console script does NOT put the cwd on sys.path (unlike the uvicorn spawn contract), so `import agento_intelligence` resolves via pytest 9's default `--import-mode=prepend` prepending the root conftest's directory (`services/intelligence/`) to sys.path — pytest 9.1.1 confirmed to still default to prepend (checked the installed source), and the recipe worked first try. **No FastAPI TestClient:** it needs httpx (not in STACK.md), so that decision is deferred to M4.1 where the real contract tests begin — the smoke instead exercises the real HTTP stack end-to-end: `uvicorn.Server(uvicorn.Config(app, host="127.0.0.1", port=7899, log_level="warning"))` in a daemon thread (port **7899, not 7891** — a dev sidecar may hold that), waiting on `server.started`, teardown via `should_exit = True` + `thread.join`, requests via stdlib `urllib.request` (`tests/test_health_smoke.py` is the only test file — the suite grows in M4.x). Three tests: valid token → 200 with the exact docs/05 §2 schema (`status == "ok"`, `version` cross-checked against pyproject.toml read independently via `tomllib`, capabilities all `false` per the "runtime facts" rule); tokenless → 403; wrong token → 403 (both asserting the exact documented detail body). Gate evidence, every command exit 0: `uv run pytest` = **"3 passed in 0.71s" from Git Bash** and **"3 passed in 0.57s" from PowerShell** (both shells per the Windows-first rule; pytest 9 header also shows `configfile: pyproject.toml` without any `[tool.pytest.ini_options]` table and `plugins: anyio-4.15.0` — anyio is a fastapi transitive that auto-registers as a pytest plugin, NOT a new dependency); `npm run typecheck && npm run lint` in `app/` green; `git status` shows only `services/intelligence/{pyproject.toml, uv.lock, conftest.py, tests/test_health_smoke.py}` — the `[tool.uv]` comment was amended to mention the dev group, and the commit carries exactly those four tracked files (PROGRESS/STACK/docs are gitignored). **The M0 milestone gate is satisfied — M0 complete:** all M0 boxes ticked, and a live `npm run dev` re-verified the window shows a chat UI with the green sidecar dot (CDP driver `phase-reports/cdp-driver-m05.mjs`, 15 s window: thread rendered, dot `status-dot--healthy` at `rgb(34, 197, 94)`, tooltip "Sidecar: healthy", zero renderer console errors; dev log at gitignored `phase-reports/m07-dev.log`); closed gracefully via the documented `taskkill /PID <main>` gesture — uv→uvicorn→python chain tree-killed, port 7891 free, no strays. **What M1.1 must know:** nothing test-side — M1.1 is Settings/safeStorage work in `app/` only; the pytest suite stays at this one smoke until M4.1 (TestClient/httpx decision lands there), pytest stays `>=9.1.1` in the dev group (uv resolves; the lockfile is the version source of truth), and `npm test` still doesn't exist in `app/` (vitest is M2.1 + STACK.md per rule 1) — ZCode
- 2026-09-07 — **M2.6 done.** `edit_file` (anchor `old_text` must match exactly once — zero/multi refusals ask for more context; always risk 2; region excerpts ±3 lines via exported `editExcerpts`) through the wrapper with snapshot-before-execute; `EditFileCard` on the `setToolUI` slot (before/after, no diff component); checkpoint after-excerpt backfill reuses the tool's own caps over stashed pre-mutation content; `changes:list` titles "Edited file"; docs/04 §4 logged same change — ZCode

- 2026-09-07 — **M2.5 done.** `create_dir` (risk 1 new / 2 existing-or-file, existed-flag snapshot, honest no-op) + `write_file` (risk exists?2:1, before/after head excerpts in result) through the wrapper; durable checkpoints (`checkpoints`/`tool_calls`, migration 0003, 10 MB/file + 200 MB/session evict-oldest) via injected `onSnapshot`/`onOutcome` sinks (agent tree stays storage-free); `changes:list` IPC + `ChangesStub` (settle-triggered refresh); WriteFileCard/CreateDirCard on the `setToolUI` slots; Groq tool-input-delta carve-out (deltas dropped — cards render from results only — fixes the append-only argsText reorder crash); docs/03 §4 + docs/04 §4 updated same commit — ZCode

- 2026-09-03 — **M0.7 reviewed (independent verification pass, no code changes needed).** Re-ran all three gates from a clean tree in PowerShell — every command exits 0: `npm --prefix app run typecheck` and `npm --prefix app run lint` green; `uv run pytest` in `services/intelligence/` → **3 passed in 0.67s** (win32, Python 3.12.3 matching `.python-version`, pytest 9.1.1, `plugins: anyio-4.15.0`). Confirmed against the installed source: pytest 9.1.1's `locate_config` in `_pytest/config/findpaths.py` first records a section-less `pyproject.toml` as `found_pyproject_toml`, then falls back to returning it as the configfile/rootdir anchor (empty config) — so the `configfile: pyproject.toml` header line is expected without a `[tool.pytest.ini_options]` table; the prepend-import recipe and import-time-token coupling in `conftest.py` both behave exactly as the Devlog's M0.7 entry documents. Commit `972c437` carries exactly the four claimed files (conftest.py, pyproject.toml, test_health_smoke.py, uv.lock); pytest (MIT) is already listed in STACK.md; no IPC or Python contract changed, so rule 6 needed no doc edit beyond this Devlog. Working tree remains clean after test runs (.venv/.pytest_cache/__pycache__ all ignored). Non-blocking nits for M4.x: (1) the test server binds a fixed port 7899 — two concurrent suites would collide; binding an ephemeral port and reading the bound socket would remove that (the choice vs 7891 is already documented here); (2) no `[tool.pytest.ini_options]` section yet — harmless because pytest 9 anchors rootdir to `pyproject.toml` regardless, but `testpaths`/`markers` belong there once M4.1 lands; (3) the M0.7 checkbox wording "(1 smoke test)" vs three test functions in the single smoke file — cosmetic; the suite intentionally stays one file until M4.1. — reviewer
- 2026-09-04 — **M1.1 done.** Settings + API key with the docs/06 §7 split, all in `app/`: new module `src/main/settings.ts` (its one deliberate Electron import is `safeStorage`; everything else pure Node, `userDataDir` passed in from `whenReady` like `db.ts`) owns two files in `%APPDATA%\Agento`: `settings.json` = `{ version: 1, provider, model }` (created on first save; absent/corrupt/unknown-model resets to defaults) and `secrets.bin` = JSON envelope `{ version: 1, keys: { <providerId>: <base64 safeStorage ciphertext> } }` written mode 0600, encrypted via `safeStorage.encryptString`, decrypted ONLY in this module on demand (plaintext keys live in a session memory map; restarts decrypt from the file). `safeStorage.isEncryptionAvailable()` = **true** on this Windows box (DPAPI), recorded in the `[settings] ready` boot log; had it been false the module keeps keys in memory and never writes secrets.bin, and the dialog honestly says "OS encryption unavailable — the key is kept for this session only". New IPC module `src/main/ipc/settings.ts`, docs/03 §4 updated same change: `settings:get` / `settings:set-api-key { provider, key }` / `settings:set-model { model }` / `settings:clear-api-key { provider }` — setters resolve void, throws keep Electron's default invoke-rejection error path, key material travels renderer→main only, and the snapshot reply is `{ provider, model, hasKey, keyLast4, storageAvailable, models }`. **Deviation:** the reply includes `models` beyond the five fields the M1.1 brief named, so `src/main/settings.ts` stays the single source of truth for the curated list and the renderer duplicates nothing. Model ids picked (verified 2026-09-03 against ai.google.dev/gemini-api/docs/models, page updated 2026-08-04; deprecated gemini-2.0-* and non-chat image/TTS/live/embedding ids excluded; default `gemini-2.5-flash`): gemini-2.5-flash, gemini-2.5-flash-lite, gemini-2.5-pro, gemini-3.5-flash, gemini-3.1-flash-lite, gemini-3.1-pro-preview. Renderer: `components/SettingsDialog.tsx` (first entry in docs/04 §4's custom-components log) — Providers section only, provider dropdown with Google AI Studio enabled + Anthropic/OpenAI disabled "later" stubs, password input whose Save swaps it for `•••• <last4>` + Remove, model `<select>` persisting on change, role=dialog/aria-modal/labelled, focus on open, Esc + backdrop close, key draft cleared from renderer state the moment main has it; entry points are a fixed chrome "Settings" text button beside the status dot and ⌘/Ctrl+, — **deviation:** the accelerator is a renderer-side DOM keydown in `App.tsx`, not an Electron Menu accelerator (no Menu exists under `autoHideMenuBar`, and pure UI state needs no new push channel); lint note: react-hooks v7's `set-state-in-effect` is an error here — setState only in async continuations of effects (a sync `setError(null)` in an effect body failed lint). Gate evidence (CDP drivers `phase-reports/cdp-driver-m11.mjs` / `cdp-restart-m11.mjs`, gitignored, no screenshots): run 1 from a clean data dir **20/20** — dialog opens via button and via a dispatched Ctrl+, keydown, Esc closes, a Google-shaped SYNTHETIC key (`AIzaSy…0001`; not a real credential, no key material in committed code) saved through the real UI path, mask `•••• 0001` replaces the input, plaintext absent from `document.documentElement.outerHTML` AND from the settings:get reply, model change to `gemini-2.5-pro` persisted, `settings.json` on disk holds `{version:1, provider:"google", model:"gemini-2.5-pro"}` with no key, the `secrets.bin` envelope's base64-decoded bytes do not contain the plaintext, zero renderer console errors; graceful quit (`taskkill /PID`, no `/F` → 0 electron/uv/python strays) → relaunch **11/11**: boot log `[settings] ready — … persistedKeys=1`, get() `hasKey: true` with the same last4, mask re-rendered without the input, plaintext still absent, sidecar dot still healthy. Synthetic key files removed after the gate so no fake key lingers for M1.2. `npm run typecheck && npm run lint` green; `uv run pytest` still 3 passed. **What M1.2 must know:** (1) the provider package (`@ai-sdk/google`) is NOT in STACK.md — that phase must add it WITH a STACK.md row in the same commit per AGENTS.md rule 1; (2) key + model are already main-side: `src/main/settings.ts` holds the plaintext key in its session map / decrypts on demand and validates `model` against the curated list — M1.2 should export an accessor (e.g. `resolveProviderKey(provider)`) for `streamText` rather than re-reading files; (3) revalidate the curated model ids against the provider package before wiring real calls; (4) the key "test" button and base-URL override stay deferred (docs/04 §3.7) along with Permissions/Appearance/Data → M6.3, onboarding → M6.1, MCP → M6.4; (5) `npm test` still doesn't exist in `app/` (vitest is M2.1 + STACK.md per rule 1) — ZCode
- 2026-09-04 — **M1.1 reviewed (independent verification pass, no code changes needed).** All gates re-run from a clean tree in PowerShell: `npm run typecheck` (node + web) and `npm run lint` exit 0; production `npm run build` exits 0 (its only stderr is the pre-existing benign zod 4.x `@__PURE__` comment warnings — PowerShell dresses them as red "errors", exit code is 0, same as the M0.6 review recorded); `uv run pytest` in `services/intelligence/` → **3 passed**. **Acceptance proven at runtime, not just by reading:** a throwaway harness (esbuild-bundled copy of `src/main/settings.ts`, `external:['electron']`, temp dir, deleted after) was loaded inside a **real Electron main process**, so `safeStorage` was genuine DPAPI — 14/14 checks passed: boot defaults; `setApiKey` trims + registers; `settings.json` never contains the key; `secrets.bin` never contains plaintext; the snapshot never leaks key material; **key persists across a simulated restart, decrypted from `secrets.bin` via real DPAPI (`hasKey:true`, `keyLast4:'5678'`)**; model persists across restart; unknown model rejected; cleared key stays cleared after restart; corrupt `settings.json` → defaults; corrupt `secrets.bin` tolerated without crash; empty key/provider rejected; short key (<8 chars) → empty `keyLast4`. That is exactly the M1.1 exit gate ("entered key persists across an app restart and is only readable in the main process") beaten in the literal terms of the docs/08 table. **Model list cross-checked** against the live `ai.google.dev/gemini-api/docs/models` page (last updated 2026-09-03, matching the Devlog claim): all six curated ids exist (`gemini-2.5-flash` / `-flash-lite` / `-pro`, `gemini-3.5-flash`, `gemini-3.1-flash-lite`, `gemini-3.1-pro-preview`), and the shut-down `gemini-3-pro-preview` plus deprecated 2.0-* ids are correctly absent. Contract/boundary compliance restated: typed preload bridge is the only crossing, no generic `ipcRenderer` passthrough, `registerSettingsIpc()` runs after `initSettings()` so handlers always answer initialized, and no new dependency entered the tree (`safeStorage` is Electron builtin) so STACK.md needed no update. **Process gap (the one real finding):** the Devlog's "docs/03 §4 updated same change" is true in the working tree but false in version history — `main`'s `.gitignore` deliberately excludes `docs/`, AGENTS/STACK/PROGRESS.md ("never pushed to GitHub"), and `backup/full-history` has not advanced past the spec-phase commit 58e4657, whose `docs/03` does **not** contain the settings IPC lines. So no commit anywhere pairs code `183cfde` with its doc update. Recommend folding the on-disk doc changes (docs/03 §4, docs/04 §3.7 + §4 log, docs/06 §7) into `backup/full-history` and keeping that branch phase-synced. Non-blocking nits, no action taken: (1) STACK.md lists vitest in devDependencies and AGENTS.md's definition-of-done names `npm test`, but `app/package.json` has no `test` script and no vitest — pre-existing, the formal JS-side unit tests belong to M4.1 as the Devlog planned, and `settings.ts` (pure Node except the single `safeStorage` import) is the prime first test target; (2) `settings.json`/`secrets.bin` writes are direct `writeFileSync` without temp+rename — read-side corruption tolerance already covers a torn write, but atomic writes would harden it later; (3) `loadPrefs` validates `provider` is a non-empty string but not membership in a known-provider list — dormant while Google is the only provider, becomes real at M1.x/provider expansion; (4) the renderer's structural mirror of `SettingsSnapshot` is typecheck-bounded (assignment compatibility fails on drift in either direction) but a shared contract type would be cleaner; (5) dialog has no focus trap (a11y — M6.3-grade polish, not M1.1 scope); (6) the `storageAvailable:false` session-only branch is code-reviewed but dormant here because DPAPI is available on this box (the honest-warning dialog branch could be exercised in M4.x via a mocked module). Working tree left clean. — reviewer
- 2026-09-04 — **M1.2 done.** The canned generator in `src/main/ipc/chat.ts` is now the real docs/02 §2.1 pipeline; the `'chat:part'` envelope, the `'session-placeholder'` constant and the verbatim `sender.send('chat:part', { sessionId, part })` forwarding loop are unchanged from M0.3 (removed: `lastUserText`/`delay`/50 ms pacing/canned part array). **Dependency:** `@ai-sdk/google` **2.0.96** pinned exact via `--save-exact`, resolved through the registry's **`ai-v5` dist-tag** — NOT `latest` (4.0.63, the ai-v7 line; `ai-v6` → 3.0.121). No peer-range conflict existed to solve: 2.0.96 declares **no `ai` peer** (only `zod ^3.25.76 || ^4.1.8`, already satisfied by the ai-trio's transitive zod 4.5.4) and its dependencies (`@ai-sdk/provider` **2.0.3**, `@ai-sdk/provider-utils` **3.0.36**) are *exactly* ai 5.0.250's own, so `google(id)` returns the `LanguageModelV2` ai 5.0.250's `streamText` expects; `ai` untouched at 5.0.250, no overrides. **STACK.md finding:** handoff item (1) was stale — on-disk STACK.md already lists `@ai-sdk/google` (Backend table grouped row + Model providers table, Apache-2.0, matching the installed package's `license` field), so no STACK.md edit was needed (verified on disk before committing). **Model-id revalidation:** `GoogleGenerativeAIModelId` is an open union (literals + `(string & {})`), so all six curated ids typecheck; noted: the union knows `gemini-3.1-flash-lite-preview` but not the bare `gemini-3.1-flash-lite` (that id was verified against Google's docs in M1.1; the live default `gemini-2.5-flash` is a union literal and worked live). **Verified API shapes (installed .d.ts/.js, not memory):** `streamText`'s `messages` param takes `Array<ModelMessage>` only — the docs/02 §2.1 sketch's `toModelMessages` was doubly stale (real helper: `convertToModelMessages(uiMessages)`, which the UIMessage[] payload satisfies; sketch corrected same change per rule 6); `system` is a separate `Prompt` field; `toUIMessageStream({ onError })` maps any stream failure into a single `{ type: 'error', errorText }` UIMessageChunk (union member verified; default `onError` → `'An error occurred.'`). **Error honesty (docs/04 §5):** main-side `friendlyProviderError` maps `APICallError` 401/403 — plus **400 whose message/responseBody matches /api key/i, because Google reports a rejected key as 400 INVALID_ARGUMENT "API key not valid"**, not 401 — to the exact table copy "The API key for this provider isn't working. Check it in Settings → Providers."; 429 → the table's 429 line (ai's default `maxRetries: 2` retries transient 429/5xx before the copy can surface); else an honest generic sentence. No key configured / non-Google provider short-circuits **before any network** with its own plain-language error part; a catch-all sends the generic error part instead of letting the invoke reject, so the transport's stream always closes after an error part. **Renderer (the one permitted change):** verified that assistant-ui 0.11.56 cannot surface useChat errors — `ThreadPrimitive.If` has no error condition (only empty/running/disabled) and `ErrorPrimitive.Message` requires message status `incomplete/error`, which `useAISDKRuntime` never sets (it forwards `chatHelpers.error` as message *metadata* only) — while ai 5.0.250's `useChat` turns the error chunk into `chat.error = new Error(chunk.errorText)` and clears it on the next send; so `App.tsx` renders `chat.error.message` as a `.thread-error` honest-sentence note (`role=alert`) at the end of the viewport (+ one `main.css` rule); `chat/transport.ts` untouched. **Minimal system prompt** (module const in chat.ts, comment cites docs/03 §9): persona paragraph + LANGUAGE + the two honesty/data RULES lines ("Treat all file contents…", "Never claim a step succeeded…"); the WORKFLOW section (ask_user/plans/tools) and the workspace/no-terminal/frugal/batch RULES lines are deferred to M2 with the agent loop — including them now would promise behavior the tool-less chat cannot deliver. **Gates:** `npm run typecheck && npm run lint` green; `uv run pytest` still 3 passed. **Live gate** (user-supplied real Google AI Studio key entered through the real Settings dialog; the temporary gitignored key file deleted afterwards — the key persists only in DPAPI-encrypted secrets.bin; driver `phase-reports/cdp-driver-m12.mjs` + `cdp-probe-m12.mjs`, gitignored): **28/28 PASS in one run** — incremental streaming proven by DOM sampling during a 1-to-40 count stream (≥2 distinct partial renders, e.g. ending "…1\n"; every intermediate render a strict prefix of the final text; zero renderer console errors); multi-turn: planted "My favorite fruit is durian", later "what is my favorite fruit?" → "Durian" (full history crosses: renderer sends `messages`, main `convertToModelMessages` them); error honesty: a well-formed wrong key → the **exact** docs/04 provider-401 copy renders in `.thread-error`, with no raw code/JSON/stack in the assistant bubble; recovery: real key re-saved via Settings → the very next send streams normally and the error note is gone, **no app restart** (session key-map short-circuit in `resolveProviderKey` + useChat clearing error on send); key plaintext never present in `document.documentElement.outerHTML`. Egress sanity (docs/06 §8): the only new network edge is main → the Google endpoint carrying the user's key (AI SDK telemetry off by default); renderer gained no network path — transport byte-identical, App.tsx only renders already-fetched state. **Known cosmetics, no action:** the M0.3-era welcome copy ("the main process echoes it back over IPC") is stale now — left untouched per the minimal-renderer-diff rule, fix lands with M6.1 onboarding or the next Thread touch; commit `feat(chat): real model streaming over the IPC transport (M1.2)` carries only `app/` files (package.json + lockfile + chat.ts/settings.ts/App.tsx/main.css) — docs/, PROGRESS/STACK, phase-reports/ are gitignored, and `backup/full-history` was fast-forwarded to main's tip + given the on-disk docs fold (AGENTS/STACK/PROGRESS/docs incl. this entry) per the M1.1 review recommendation. **What M1.3 must know:** (1) sessions schema is docs/03 §8 (`sessions`: id TEXT PK, title DEFAULT 'New task', **workspace_path NOT NULL** → store an empty-string placeholder until the M2.2 picker and say so in the Devlog, mode DEFAULT 'auto', status DEFAULT 'idle', provider/model nullable, created_at/updated_at NOT NULL); (2) adopt **temp+rename atomic writes** for any NEW persistence code per the M1.1 review nit (settings.json/secrets.bin keep their reviewed `writeFileSync` until a hardening pass — don't mix styles silently); (3) the chat pipeline reads `getSettings()` + `resolveProviderKey(provider)` fresh on every message — M1.3 should stamp provider/model from the same snapshot when creating a session row; (4) `'session-placeholder'` is duplicated main (`ipc/chat.ts`) and renderer (`chat/transport.ts`) — real session ids in M1.3 replace both and the renderer's sessionId filter becomes load-bearing for the first time; (5) `storage/db.ts` singleton + better-sqlite3 12.11.1 are ready; **drizzle is named in docs/03 §8 but NOT installed** — decide at M1.3 start whether drizzle lands (then STACK.md per rule 1) or the schema goes through plain better-sqlite3 (then docs/03 §8 per rule 6); (6) `npm test` still doesn't exist in `app/` (vitest is M2.1 + STACK.md per rule 1) — ZCode

- 2026-09-04 — **M1.3 done.** Sessions + sidebar, all in `app/`: **drizzle lands** — `drizzle-orm@0.45.2` + `drizzle-kit@0.31.10` pinned exact from the lockfile (licenses verified against the registry: orm Apache-2.0, **kit MIT** — the STACK.md row said Apache-2.0 for both and was corrected same change; better-sqlite3 untouched at 12.11.1). **Schema/migration:** first migration `app/drizzle/0000_*.sql` creates ONLY `sessions` + `messages` per docs/03 §8 (`drizzle-kit generate`; SQL + meta committed; `db.ts` gains `PRAGMA foreign_keys = ON` at open, exposes a typed drizzle instance, and runs drizzle's better-sqlite3 `migrate()` with `migrationsFolder` passed in from `src/main/index.ts` via `app.getAppPath()` — dev = `app/`; **packaged-path question deferred to M6.6**; the M0.6-era empty DB migrated cleanly, `__drizzle_migrations` bookkeeping created on first boot). Repos in `src/main/storage/sessions.ts` are the only DB-touching code: `createSession` / `listSessions` (updated_at DESC) / `getSession` / `getSessionMessages` / `appendMessage`. **Interpretations recorded:** `messages.content` stores the full serialized UIMessage JSON (role from the UIMessage; `seq` = per-session MAX+1; INSERT … ON CONFLICT(id) DO NOTHING idempotent against re-sends); `workspace_path` stores `''` — the mandated NOT NULL placeholder until the M2.2 picker; title set from the first user message truncated to 60 chars, single line; provider/model stamped at creation from `getSettings()`. **Chat flow:** `chat:send` payload gains `sessionId` (`'session-placeholder'` retired on BOTH sides; envelope shape unchanged, now carrying the real id); main persists the user message BEFORE streaming and the final assistant UIMessage AFTER the stream completes via new `src/main/ipc/assistant-accumulator.ts` (text-start/delta/end state machine; error parts mark the stream failed → nothing persisted assistant-side; nothing mid-stream). **Renderer session identity:** App owns `activeSessionId` (ref = the transport's source of truth, state mirrors it for the highlight) + a `threadEpoch` advancing ONLY on explicit New chat / open; `ChatView` is keyed by epoch and seeds `useChat({ messages })` from `ChatInit.messages` (verified against @ai-sdk/react 2.0.253) — remount-per-view chosen over a setMessages singleton because a singleton would land an in-flight stream's parts in whatever session it points at; the transport (`createIpcChatTransport`) captures the session id per send and filters `chat:part` by it, so streams can never leak across sessions. Lazy creation: New chat starts with NO session; the first send runs `session:create` — empty sessions never reach the sidebar. `SessionsSidebar` component + `#root` row layout in `main.css` (docs/04 §4 custom-components log updated). **Deviation — Groq provider added (sanctioned by STACK.md's pre-existing provider row; lockfile + code this change):** Google free-tier daily quota (20 req/day/project/model) exhausted mid-verification, blocking every live send; added `@ai-sdk/openai-compatible@1.0.53` exact-pinned — the ONLY release line matching the installed `ai@5.0.250` / `@ai-sdk/google@2.0.96` provider spec (`@ai-sdk/provider@2.0.3`; openai-compatible 2.x/3.x target provider v3/v4 and would break `streamText`; Apache-2.0) — plus `settings:set-provider { provider }` IPC and a snapshot `providers` field (main = source of truth; the renderer renders disabled stubs for the rest); curated Groq list: `llama-3.3-70b-versatile` (default), `llama-3.1-8b-instant`, `openai/gpt-oss-120b`, `openai/gpt-oss-20b`, `qwen/qwen3.6-27b`, `qwen/qwen3.8-27b` (chat/tool-capable only, verified 2026-09-05 via the models.dev catalog). **Bug found by that switch, fixed:** replayed assistant history carrying `reasoning` parts (thinking models; main forwards stream parts verbatim so the renderer's history includes them) is rejected on input by OpenAI-compatible providers (Groq: "property 'reasoning_content' is unsupported") — `chat.ts` now strips reasoning parts from incoming assistant messages before `convertToModelMessages` (`replayable()`); reasoning never persists (the accumulator is text-only), so DB rows were always clean. **Gates:** typecheck + lint green; `uv run pytest` still 3 passed. **Live gate** (Groq key entered through the real Settings dialog; driver `phase-reports/cdp-driver-m13.mjs`, probe `m13-diag.mjs`, DB dump under Electron-as-Node — all gitignored): fresh-DB **run1 20/20** (lazy creation, titles, relative timestamps, active highlight, updated_at DESC ordering, reopen with no cross-contamination, follow-up remembering from persisted history), full-quit **run2 9/9** (full conversation restored + mid-conversation send), second-restart **run3 10/10** (post-restart turn in storage, session 2 independent), zero renderer console errors in all three; DB dump on the final state: 2 sessions, 10 messages, seq + ISO-8601 timestamps + `workspace_path: ''` + provider/model stamps correct, `integrity_check ok`, `foreign_key_check []`. **Deferred:** session delete/rename → later phase; zustand → M3; packaged migrations-folder path → M6.6. **What M1.4 must know:** (1) the accumulator + `toUIMessage()` snapshot are structured for partial persistence on abort — persisting a PARTIAL assistant message cleanly is the M1.4 goal ("stopping mid-stream leaves a clean, resumable session"); (2) `appendMessage`'s ON CONFLICT(id) DO NOTHING needs upsert-or-replace semantics so a re-persisted partial assistant message REPLACES its earlier row; (3) the transport's currently-ignored `abortSignal` becomes load-bearing — wire AbortController end-to-end; (4) thinking models emit long reasoning-delta runs before any text (observed ~57 deltas on gpt-oss-120b) — stop-button UX must account for the long lead-in. — ZCode
- 2026-09-05 — **M1.4 done.** Stop button aborts the run end-to-end, and a stopped run is a first-class outcome, not a failure. **Stop wiring (transport → `chat:stop` → `AbortController` → `streamText`):** an AbortSignal cannot cross IPC, so the chain is — composer/Esc → `chat.stop()` (useChat aborts exactly the per-send controller whose signal it handed the transport, verified in ai@5.0.250 dist: `Chat.stop` aborts `activeResponse.abortController`, `sendMessages` receives `response.abortController.signal`) → the transport's abort listener (the previously-ignored `abortSignal` param is now load-bearing) → `invoke 'chat:stop' { sessionId }` → main's module-scope `Map<sessionId, AbortController>` → `controller.abort()` → `streamText({ abortSignal, onAbort })`. **Abort signals verified against the installed ai@5.0.250 dist, not memory:** `streamText` takes top-level `abortSignal` + `onAbort` (dist/index.d.ts:1343/1439); the UI message stream natively enqueues `{ type: 'abort' }` on abort (dist/index.js:5186); `UIMessageStreamOutcome` has `'aborted'` and the finish callback carries `isAborted` (d.ts:1858–1882) — main forwards the native abort chunk verbatim (never synthesizes a duplicate) and treats an `error` chunk arriving while the signal is aborted as the abort signature, so an abort never surfaces the generic provider copy. **Terminal-part discipline (docs/02 §2.1 wire contract, updated same change):** every run ends with exactly one terminal part — `finish` (the provider's own part is HELD and forwarded only after persistence succeeds), `abort` (after the partial is persisted), or `error` (provider failure, or a persistence failure: a save throw REPLACES the finish/abort terminal with `PERSIST_FAILED_COPY` 'The reply could not be saved to this conversation.' — persistence failures are always loud, never swallowed); the map entry is deleted in a `finally` as the forwarding loop exits, so a stop landing during the persist window — or when nothing is running — is a no-op. **Partial persistence + replace semantics:** on abort main persists `accumulator.toUIMessage()` (null during the reasoning lead-in → nothing persisted; the user message is already saved, so the session stays clean and resumable); `appendMessage` went `ON CONFLICT(id) DO NOTHING` → `DO UPDATE` replacing content/role/seq/created_at in the same transaction (sessions.updated_at still bumps on write); drizzle migration **0001_left_solo.sql** adds `UNIQUE(session_id, seq)` (single `CREATE UNIQUE INDEX`, applied by the existing startup `migrate()`; no existing data could violate it since seq is computed max+1 per transaction). **In-flight / one-row guard:** the per-view transport rejects a second `sendMessages` with `SECOND_RUN_COPY` while a run is active and shares one in-flight `session:create` promise so a fast double-send on a new chat makes exactly ONE row — and the load-bearing discovery: **useChat has no running guard and a second request can reach the transport with the renderer flag cleared (observed live pre-fix: two concurrent streams, finish×2), so serialization is ALSO enforced main-side** — `chat:send` rejects with `SECOND_RUN_COPY` while the session has an active run, before persisting anything; docs/02 §2.1 + docs/03 §4 updated same change (reserved `run/stop` became the real `chat:stop { sessionId }`; docs/03 §8 gained the UNIQUE index + upsert/partial-persistence semantics). **Stop UX:** Send/Stop swap via `ThreadPrimitive.If running={false}`/`running` around `ComposerPrimitive.Send`/`ComposerPrimitive.Cancel` (docs/04 §3.5) — `canCancel` is a capability flag (`onCancel !== undefined`, always true under `useAISDKRuntime`), so the If gate is load-bearing; `isRunning` covers status `submitted`, so Stop is visible through the reasoning lead-in (~57 reasoning deltas before any text on gpt-oss-120b — the M1.3 diagnostic reconciled: ComposerSend's disabled = `isRunning || !isEditing || isEmpty` DOES track running, it just was never a completion *signal*); Esc stops via a document keydown in ChatView that skips while the Settings overlay owns Esc. **Unmount cleanup:** `createIpcChatTransport` now returns `{ transport, dispose }` — dispose aborts the view's active run (partial persisted main-side) and unsubscribes its `onPart`; it is invoked by App's `openSession`/`startNewChat` BEFORE the remount via a `chatDisposeRef`, NOT in an effect cleanup — the first gate attempt caught StrictMode's simulated mount-unmount permanently disposing the transport (every send rejected, zero parts); a `cancel()` source callback frees run state when useChat cancels the stream itself. **Gates:** `npm run typecheck && npm run lint` green; `uv run pytest` still 3 passed; 4-run CDP sequence (`phase-reports/cdp-driver-m14.mjs` + `m14-gate.ps1`, Groq openai/gpt-oss-120b, fresh DB, key from M1.1 storage entered through the real Settings dialog): run1 **20/20** (stop during the lead-in with zero text rendered AND during visible text — native abort part, no error part, stream stops promptly with 0 parts after abort, partial stays on screen and prefix-extends the pre-stop capture, composer re-enables, no thread-error card), restart run2 **10/10** (both stopped sessions listed; the visible-text partial restored from storage; follow-up sends continue cleanly in both), run3 **10/10** (double-send: ONE sidebar row, finishes=1, coherent reply; second send during an active run: exactly one run on the wire), run4 **10/10** (mid-stream switch: native abort part, no ghost listeners, no stuck thinking on either view, partial persisted by the switch-abort restored and extending the on-screen prefix); zero renderer console errors in every run. **DB sanity** (`db-dump-m14.mjs` under Electron-as-Node, app closed): migrations 0000+0001 applied, `messages_session_seq_unique` present, zero duplicate (session_id, seq), integrity ok + FK clean; Lighthouse (lead-in stop) has NO assistant row at the stopped turn, Bridges and Volcanoes carry partial assistant rows at seq 2, Canals/Instruments exactly one turn each. **Two benign edge races accepted:** an unmount inside the ~ms lazy-create window leaves an empty session row (the shared createPromise recovers it on the next send); a guard-rejected second send leaves its user bubble in the thread until settle (never persisted — vanishes on restart). **Deferred:** regenerate/retry of a stopped run → later; session rename/delete → later. **What M1.5 must know:** (1) token usage is per-session — `result.usage` / `onFinish` on a NORMAL run; an aborted run has partial usage (`onFinish` carries `isAborted`/`outcome` if usage-on-abort is ever wanted) — keep this phase's persistence shape so a `usage_events` row can ride alongside a persisted message, and hook usage at main's settle point (main persists BEFORE the terminal part goes out; `appendMessage`'s id upsert makes a re-persist a replace, not a dupe); (2) renderer StrictMode rule: never dispose/teardown in mount-time effect cleanups — use the `registerDispose` ref pattern driven by explicit App-level switches; (3) `ComposerPrimitive.Cancel`'s enabled state is capability-only — anything Cancel-like must be gated by `ThreadPrimitive.If running`; (4) the docs/02 §2.1 chat:send sketch now shows the hold-finish/abort discipline — keep it in sync when tools (M2.x) add approval pauses to the same loop — ZCode
- 2026-09-05 — **M1.4 evidence-record correction (review pass, no code changes).** The final gate sequence's db-dump step failed 2 checks on the first attempt (`db-dump-m14.mjs` assertions were over-strict — they didn't anticipate the follow-up turn's additional rows: Lighthouse legitimately carries the follow-up's assistant reply at seq 3, Bridges more than the stopped turn). The app code and DB rows were already correct; the script's expectations were corrected and re-run against the same DB — **ALL CHECKS PASSED**; the four CDP runs and zero-console-errors claims were unaffected. Disclosure mirrored in `phase-reports/M1.4-stop-button.md`. — Cline
- 2026-09-05 — UI enhancement planning pass (docs-only, no code): M1.6 (design-system foundation) + M1.7 (transcript polish) added to M1 in docs/08 + here, before M2, so M2/M3 cards and panels build on the design system instead of being restyled at M6; docs/04 gained **§8 Design system & visual polish** (§8.1 Tailwind v4 + shadcn tokens light/dark-ready, §8.2 styled scrollbars — previously unspecified, the native-scrollbar complaint, §8.3 message markdown via `@assistant-ui/react-markdown` + `remark-gfm`, §8.4 motion) and its §4 fallback ladder gained a licensed-registry step: **prompt-kit (MIT, license verified at source) is the primary component registry; AI Elements (Apache-2.0, verified) is pattern-reference only (Next.js-targeted)**; STACK.md gained 4 planned deps (remark-gfm, @fontsource-variable/inter, use-stick-to-bottom, motion — licenses verified: MIT/MIT/MIT/MIT; the first two unconditional, last two conditional) + a copy-paste-sources note; AGENTS.md rule 4 amended to match docs/04 §4; docs/08 sub-phase count 49→51; README doc map + docs/06 §9 IP-hygiene line updated for the registry step; **Current phase unchanged: M1.5 comes first** — ZCode
- 2026-09-05 — **M1.5 done.** Token usage recorded and shown per session, and the first session-scoped `agent:event` is on the wire. **Migration 0002** (`drizzle/0002_*.sql`, drizzle-kit generated) adds ONLY the `usage_events` table with the exact docs/03 §8 columns (id TEXT PK, session_id FK, nullable input_tokens/output_tokens INTEGER, created_at TEXT NOT NULL); repos stay the only DB-touching code (`storage/usage.ts`: `insertUsage` + `getUsageTotalsBySession`, one grouped sum query — no N+1). **zod@4.5.4 lands as a DIRECT dep pinned exact** — the version already resolved transitively (ai trio + @assistant-ui dedupe to it; satisfies ai's peer range `^3.25.76 || ^4.1.8`; single copy, no peer conflict); needed because docs/03 §4's Zod-validated-both-sides rule becomes load-bearing with the first `agent:event` (main schema in `ipc/agent-events.ts` parses before send, renderer mirror `chat/agent-events.ts` validates before state); STACK.md needed no edit — the zod row predates this phase. **Envelope semantics (M1.5 simplifications, recorded in docs/03 §4 in the same change per rule 6):** `runId` is a per-send uuid generated in main at `chat:send` (real run lifecycle arrives M2); `seq` is a per-session monotonic counter kept in main memory only (starts at 1, resets on restart) — the persisted event log / replay semantics arrive with the session state machine; `ts` is epoch ms. Channel is a broadcast to all windows (sidecar:status precedent — avoids stale blurred windows; renderer filters by sessionId). **What actually happens to usage on an abort — verified against the installed ai@5.0.250 dist, not memory:** `streamText`'s `onFinish` fires only when ≥1 step completed; a mid-first-step abort (the only abort shape without tools) never fires it, `result.totalUsage`/`usage`/`steps` REJECT with `NoOutputGeneratedError`, `onAbort({steps})` yields `steps: []`, and `toUIMessageStream`'s `onFinish` (which does carry `isAborted`/`outcome` — that's where the M1.4 handoff's flags actually live) carries no usage either. So a stopped run records NOTHING — no row, no event — honestly, never a fabricated zero; usage is captured via the `onFinish` callback into a local (it runs inside the stream's flush, before the forwarding loop exits, so it is settled by the settle point) and `totalUsage` is never awaited unguarded. The SDK also reports `reasoningTokens`/`cachedInputTokens`/`totalTokens` on `LanguageModelUsage`; those are NOT in the documented schema and are dropped — only input/output stored (captured from `event.totalUsage`; single-step runs make it equal `usage`). **Placement note:** docs/03 §4's consumer for `usage` says "header meter" — no header chrome exists yet, so the per-session totals live in the SessionsSidebar rows (minimal `.session-item-usage` CSS) this phase; the header meter is deferred until that chrome exists (M1.6/M1.7 restyle will relocate freely). `session:list` rows now carry `usage { inputTokens, outputTokens } | null` (one grouped query; `session:create` returns `usage: null`) — the response shape is mirrored in preload `index.ts`/`index.d.ts` and `renderer/chat/transport.ts`, keep the mirrors in sync. App-level `agent:event` listener uses the passive-listener pattern (mount effect returning unsubscribe as cleanup — StrictMode-safe since resubscription is complete); the `registerDispose` ref pattern stays reserved for the transport's destructive dispose. A usage-bookkeeping failure logs main-side and never replaces the terminal part (only reply-persistence failures are loud per docs/02 §2.1). **Gates:** `npm run typecheck && npm run lint` green; `uv run pytest` 3 passed; CDP gate run1 20/20 + run2 5/5 + db-dump ALL CHECKS PASSED (two driver bugs fixed between attempts — probe array reset hiding a session's event, and an assertion on `__drizzle_migrations.tag` which the sqlite migrator never stores; app code unchanged and correct throughout; the gate's dump step also gained a one-retry for a spawn-timing race after Stop-Dev's force-kill); zero renderer console errors in both runs; egress sanity: new code is IPC + SQLite only. Report `phase-reports/M1.5-token-usage.md`. **What M1.6 must know:** Tailwind v4 + shadcn init is CSS-first (`@theme` tokens, `components.json`) — dark is the default token set (light exists; the user-facing switcher arrives M6.3, checked by temporarily flipping the variable set); the four planned STACK.md deps are remark-gfm + @fontsource-variable/inter (unconditional) and use-stick-to-bottom + motion (only if assistant-ui doesn't cover scroll anchoring / a copied prompt-kit component needs it) — all already have rows, licenses verified MIT; **prompt-kit (MIT) is the primary copy-paste registry under amended rule 4** (AI Elements is pattern-reference only); styled scrollbars on EVERY scroll region (sidebar list, thread viewport, settings body, composer textarea — no native Chromium scrollbar visible); the sidebar usage line and all existing chrome restyle onto tokens with no behavior changes — ZCode
- 2026-09-05 — M1.5 backup-sync note: the `backup/full-history` phase-sync commit folds the on-disk AGENTS.md/STACK.md/PROGRESS.md + docs/ (which had drifted two phases — M1.3/M1.4 never synced) as of main `0c67d9c`; **`phase-reports/` deliberately NOT folded** (human decision this phase — reports stay working-tree-only) — ZCode
- 2026-09-05 — **M1.5 hardening (review pass).** Reviewer found the sidebar's live usage total could in principle double-count: the renderer added each `agent:event`'s tokens provisionally and relied on the settle-triggered session:list refresh to replace them — correct only if the push event always beats the invoke reply, and push-vs-invoke ordering is not guaranteed (the M1.3 transport note records an invoke reply overtaking trailing pushes). Took the delete route: the provisional add-path is gone (`App.tsx` listener now validates every event — the Zod-both-sides rule — and holds the M3 subscription lifecycle, but never mutates totals); totals are owned by the settle-triggered session:list refresh, which reads the authoritative `usage_events` sums, so double-counting is structurally impossible. The event stays emitted + validated both sides — the "first agent:event" deliverable and channel plumbing untouched; only visible change is the row updating at the settle instant instead of a few ms earlier. Full gate re-run on the hardened code: run1 20/20, run2 5/5, db-dump ALL CHECKS PASSED. Gate-harness corrections made in the same pass (local scripts only): (1) my earlier "spawn-timing race after Stop-Dev" diagnosis was WRONG — the real cause of the dump's flaky `db dump failed` throws is that `electron.exe` is a GUI-subsystem binary, so PowerShell neither sets `$LASTEXITCODE` nor captures output when invoking it directly; the gate now routes the dump through `cmd.exe /c` (which waits for GUI children and propagates their exit code) and also asserts the `ALL CHECKS PASSED` output marker; (2) the dump opens the DB read-write on purpose — `taskkill /F` leaves an uncheckpointed WAL that a READONLY connection cannot recover (stale `-shm`), so the open itself failed intermittently; the open/close performs the recovery and the script still only executes reads; (3) gate `.ps1` files must stay ASCII-only — a UTF-8-no-BOM em-dash broke PowerShell 5.1's ANSI parse (a `m15-gate.ps1` retry-loop edit briefly made the whole script unparseable) — ZCode
- 2026-09-06 - **M1.6 done.** Design-system foundation: Tailwind v4 + shadcn tokens, self-hosted Inter, styled scrollbars, chrome restyled with no behavior changes. **Versions (lockfile-resolved):** tailwindcss 4.3.3, @tailwindcss/vite 4.3.3 (the v4 Vite plugin, required under electron-vite - new STACK.md row), @fontsource-variable/inter 5.3.0, clsx 2.1.1 + tailwind-merge 3.6.0 (cn() deps - new STACK.md rows; licenses verified MIT at source; @fontsource's OFL-1.1 font files already covered by its row). class-variance-authority / tw-animate-css / lucide-react NOT installed - shadcn's current installer (v4.21.0, npx-only, not a project dep) adds those with the first component adoption; rows land then. **shadcn init fallback (deviation, within the approved plan):** the current CLI classifies electron-vite as framework "Manual" (reads tsconfig.json/tsconfig.app.json, not our tsconfig.web.json) and cannot locate the CSS non-interactively; with components.json present it only offers an interactive overwrite. Authored the exact installer artifacts against the official schema.json instead - components.json (style new-york, baseColor neutral, css src/renderer/src/assets/main.css, aliases @/components @/lib/utils @/components/ui @/lib @/hooks), src/renderer/src/lib/utils.ts cn(), @/* alias in tsconfig.web.json paths + electron.vite.config.ts resolve (alongside @renderer), tailwindcss() in the renderer plugins - and verified via `npx shadcn@latest info`, which fully resolves the config. **CSS restructure:** base.css deleted (--ev-c-* retired; unused --ev-button-alt-* gone); main.css is the single CSS-first entry: @import 'tailwindcss' + '@fontsource-variable/inter', @custom-variant dark, :root light set / .dark dark set (both with color-scheme so native select popups follow the theme), @theme inline mapping --color-*/--radius-*/--font-sans ('Inter Variable' first). **Token names:** shadcn standard (--background, --foreground, --card/-foreground, --popover/-foreground, --primary/-foreground, --secondary/-foreground, --muted/-foreground, --accent/-foreground, --destructive/-foreground, --success/-foreground, --warning/-foreground, --border, --input, --ring, --radius); neutral oklch grays (dark bg 0.145 / card 0.205; light 1.0 / 0.97); indigo --primary (dark indigo-500, light indigo-600) + --ring; Section 7's semantic red IS --destructive (no duplicate --danger); --success/--warning added for the sidecar dot and future risk accents. **Dark default** ships as <html class="dark"> in index.html; light = removing the class (driver-verified); no switcher UI exists (M6.3). **Restyle:** every existing class name kept, zero TSX changes (the .settings-overlay querySelector and status-dot--${status} template stay valid; no behavior changes); Send + New chat are the only indigo surfaces (user decision: primary actions + focus only); Stop/settings/bubbles/rows neutral (neutral hover = color-mix(foreground 8%, secondary)); thread-error + unhealthy dot moved off hardcoded reds onto --destructive/--success; settings dialog lost its box-shadow (hairlines over Section 7). **Scrollbars (Section 8.2):** one block in main.css for .thread-viewport/.sessions-list/.settings-dialog/.composer-input - 8px token-themed thumb (var(--border), hover muted-foreground 45% mix), transparent track, horizontal variant, scrollbar-gutter stable; the scrollbar-width: thin fallback line sits inside @supports not selector(::-webkit-scrollbar) because Chromium 121+ disables webkit scrollbar styling when scrollbar-width is non-initial (bare thin would surface the NATIVE scrollbar). **CSP unchanged:** Inter loads same-origin (woff2 exceeds the inline threshold; no data: URLs), zero console errors. **Gate:** fresh-DB functional1 12/12 (stream 235 in / 89 out, stop records nothing), relaunch functional2 15/15 (usage restored identically; settings key cycle - secrets.bin backed up pre-launch and hash-restored post-gate because all providers are keyed, so the dialog's remove-then-save-then-remove was exercised safely), visual 37/37 (dark default, light flip, reload restore, all-localhost egress), typecheck + lint green, pytest 3 passed. Driver note: .settings-dialog must be opened before its scrollbar check (dialog only exists while open). **What M1.7 must know:** (1) markdown wiring point = MessageText (App.tsx:18-20) via MessagePrimitive.Parts - replace with @assistant-ui/react-markdown + remark-gfm; keep .message-text's user-select: text; prose styles on the token layer (links --primary, monospace only where content is code). (2) prompt-kit adoption begins under amended rule 4 - Thinking-Bar/Loader pattern for the thinking indicator, assistant-ui primitives first; any npm dep a copied component imports gets a STACK.md row in the same commit. (3) use-stick-to-bottom only if assistant-ui doesn't cover scroll-to-bottom; motion only if a copied component requires it (CSS keyframes within the 150-200ms budget otherwise; prefers-reduced-motion must disable added motion). (4) Token utilities work via @theme inline (bg-success/text-warning included); new scrollable panels join the scrollbar selector list at the bottom of main.css. (5) The secrets.bin backup/restore gate pattern is reusable for any phase that exercises key save/remove - ZCode
- 2026-09-06 - **M1.7 done.** Transcript polish: streaming markdown for assistant replies, thinking indicator over the reasoning lead-in, scroll-to-bottom button, 150ms enter motion with reduced-motion off-switch — plus the user-directed neutral accent. **Version gate (the M0.2 lesson, honored):** pinned trio untouched (`ai` 5.0.250 / `@assistant-ui/react` 0.11.56 / bridge 1.1.21); installed `@assistant-ui/react-markdown` **0.11.9** + `remark-gfm` **4.0.1** exact — 0.11.9 is the *highest* markdown package accepting 0.11.56 (peer `^0.11.53`; 0.11.10 needs `^0.11.58` = the broken version, 0.14.14 needs `^0.15.0`); resolved transitives `react-markdown` 10.1.0 + `classnames` 2.5.1 (all MIT, transitive rows of an approved STACK.md entry — no new rows). **Ladder decisions (docs/04 §4):** markdown = `MarkdownTextPrimitive` wrapper (core ships no renderer); thinking indicator = prompt-kit **Loader** (MIT) adopted and trimmed to the dots variant — assistant-ui has no indicator primitive and `InProgress` is blind to the reasoning lead-in; Thinking-Bar rejected (lucide-react + text-shimmer deps); scroll-to-bottom = custom build on `ThreadPrimitive.ScrollToBottom` (assistant-ui covered it — **`use-stick-to-bottom` conditional row did NOT materialize**); motion is CSS keyframes only (**`motion` row did NOT materialize**). **User decision 2026-09-06 — app-wide neutral accent:** Send + New chat turned gray via `--primary`/`--ring` token change (light `oklch(0.42 0 0)` / dark `oklch(0.72 0 0)` + flipped `--primary-foreground`), superseding M1.6's indigo; docs/04 §7/§8.1/§8.3 corrected in the same commit, M1.6 report left as historical record. **Gate:** CDP main 39/39 (markdown DOM, indicator window, scroll/copy round-trip, achromatic accents, zero errors), reduced 8/8, restore 6/6; db-dump schema/shape unchanged from M1.5 (text-only parts, no HTML); typecheck + lint green; pytest 3 passed. **Harness note:** this machine has Windows client-area animations disabled, so Chromium always reports `prefers-reduced-motion: reduce` — motion-emulating driver modes must emulate `no-preference` (restore proven on a fresh launch). Stopped essay turns occasionally persisted no partial: a mid-stream provider error sets the accumulator's failed flag and legitimately persists nothing (M1.3 semantics) — dump treats this as provider-dependent, not a regression. **M1 milestone gate met by construction** (every M1.* box ticked). **What M2.1 must know:** vitest lands with the tool-registry tests (add its STACK.md row then); UI carries no M2 blocker; the accent is now neutral gray (`--primary`) — action-card risk accents stay `--destructive`/`--warning`/`--success`. — ZCode
- 2026-09-06 — **M2.1 done.** Tool registry + wrapper (validate → sandbox → risk → approval-hook → snapshot → execute → truncate) proven on the docs/03 §5 example `write_file`, with the first test runner in `app/` (vitest). **Architecture (`src/main/agent/`, plain Node TS, zero Electron imports):** `types.ts` defines `ToolDefinition` (name/description/access/`inputSchema` Zod/`pathFields`/risk/describe/execute — one definition drives the LLM schema, the card, the risk gate, and the logger per docs/03 §5), `ToolExecutionContext` (workspaceRoot/exists/snapshot/requestApproval/fs — the injected subset of docs/03 §5's ctx; storage repos/intelligence client/event sender connect in M2.5/M3), `SnapshotStore`, `WorkspaceFs`; `registry.ts` is the wrapper — schema rejection short-circuits before ANY other stage, the sandbox pre-resolves every declared `pathFields` entry to an absolute workspace path (tools never see raw model paths — docs/06 §4), risk ≥ 1 or write-access ⇒ mandatory snapshot BEFORE execute (no opt-out, docs/03 §7), risk ≥ 2 ⇒ approval hook with `skip`/`cancel` returning honest non-executions (no snapshot, no mutation), result truncation at 8 KB (`MAX_TOOL_OUTPUT_BYTES` — docs/03 §5). **AGENTS.md rule 2 done structurally, not by discipline:** tool bodies never import `node:fs`; `workspace-fs.ts` provides the only disk facade, it re-checks containment on every call (defense in depth behind the pre-resolved paths) and writes temp-file + atomic rename on the same volume (docs/06 §4.5), refusing leaf escapes with plain language. **Kept out of M2.1 (recorded decisions):** `toAiSdkTools(ctx)` deferred to M2.4 (verified the ai v5 shape is the `tools` field of `streamText`; M2.4 confirms against the installed 5.0.250 `.d.ts`); snapshot stage is an in-memory store (`snapshots.ts`) — the durable SQLite `checkpoints` table + content store lands at M2.5 whose checkbox explicitly says "first run proves snapshotting"; approval coalescing, per-step groups, and the modal UI are M3 (the wrapper exposes the hook contract now). **Vitest:** `npm i --save-exact -D vitest` → **5.0.0** (license MIT, devDep); `"test": "vitest run"`, `vitest.config.ts` (node environment, `include: ['src/main/**/*.test.ts']`), `tsconfig.node.json` include gains `vitest.config.*`. STACK.md finding: the M1.7 handoff said "add its STACK.md row then" — **on-disk STACK.md already lists vitest** (line 56, the `typescript / vitest / eslint` devDependencies row), so no STACK.md edit was needed (verified before committing, same as the M1.2 `@ai-sdk/google` finding). **Gate (both shells):** PowerShell — typecheck 0, lint 0, vitest 19/19 (4 files), pytest 3 passed; Git Bash — typecheck/lint/test exit 0 each, pytest exit 0. Commit `bac103d` carries exactly `app/package.json`, `app/package-lock.json`, `app/tsconfig.node.json`, `app/vitest.config.ts`, `app/src/main/agent/**` (16 files, 1241 insertions). **Docs:** no doc edit needed — no IPC/Python contract changed (rule 6 n/a); the wrapper's ctx grows in later phases without breaking this shape. **What M2.2 must know:** (1) the workspace picker resolves a REAL root for `ToolExecutionContext.workspaceRoot` — the sandbox string rules (`resolveWorkspacePath`) and the fs facade are already built on `workspaceRoot` and just consume whatever main provides; the M2.3 realpath walk compares junction escapes against `realpath(workspaceRoot)`, so M2.2 should store the picked path AFTER `realpath` so junctions in the picked path don't surprise the guard; (2) `sessions.workspace_path` stays `NOT NULL` with the `''` placeholder until the picker stamps real values — M2.2 flips `createSession` to accept `workspacePath?` and the IPC handler stamps `getCurrentWorkspace() ?? undefined`; (3) `npm test` now exists — every sub-phase runs it in both shells from here on; vitest 5.0.0 is exact-pinned, do not bump; (4) the `testing/harness.ts` (temp workspace + scriptable approval hook + stage-order log) is the pattern for every future tool suite — reuse it rather than re-inventing per-phase fixture code; (5) **branch topology finding:** `backup/full-history` and `main` have no merge-base — the backup branch is a sibling lineage carrying docs only (`docs: phase-sync on-disk specs through M1.x (main <hash>)` commits), so the brief's literal "fast-forward backup/full-history to main" is impossible; the actual M1.x precedent (verified in the git history) is a docs commit on backup referencing the main hash, and this session follows that precedent. Note also: git checkout main→backup overwrites on-disk docs to backup HEAD (they are untracked on main), so docs edits happen ON the backup branch before that branch's commit. — ZCode
- 2026-09-06 — **M2.2 done.** Workspace picker (native dialog), recents, workspace context on sessions. **Modules:** `src/main/workspaces.ts` (plain Node, zero Electron imports, userDataDir injected like `db.ts`) owns the current workspace + recents in `<userData>/workspaces.json` (`{ version, current, recents: [{ path, lastOpenedAt }] }`, cap 10, most-recent-first, de-duped case-insensitively on win32) — written temp-file + atomic rename per the M1.2 handoff nit, absent/corrupt resets to empty, and the stored path is **realpath-canonicalized** on set (junctions folded, Windows-cased) so M2.3's sandbox walk compares against the exact same base (picking through a junction cannot surprise the guard). `src/main/ipc/workspaces.ts` (Electron) registers `workspace:get` / `workspace:list` / `workspace:pick` / `workspace:set`: the **native folder dialog is owned by main** (`dialog.showOpenDialog`, `openDirectory`), pick resolves `{ path }` or `null` on cancel, and `workspace:set` only re-applies paths this module produced (recents) — a renderer can never inject an arbitrary path as a workspace. `session:create` now stamps `workspace_path: getCurrentWorkspace() ?? undefined` (the `''` placeholder survives until a pick), and `SessionInfo`/`SessionSummary` carry `workspacePath` end-to-end (preload d.ts + renderer mirror). **UI:** `WorkspacePicker` component at the top of the sessions rail — chip (uppercase label + truncated path, honest "No workspace selected" empty state) → click opens the recents menu (path + relative time per entry, "Choose folder…" pinned at the bottom); errors render as a `.workspace-error` line, never a crash. **Testability finding:** better-sqlite3 is Electron-ABI on this box — plain Node (and therefore vitest) cannot load it (`NODE_MODULE_VERSION` mismatch), so DB-touching proofs stay out of vitest; the store is fully unit-tested there (7 tests) and the DB attach proof is an esbuild-bundled harness run inside a real Electron main (`phase-reports/m22-attach-driver.ts` → `app/out/m22-attach-bundle.cjs`, `M22_ATTACH_OK`: pick→persist→realpath, session:create composition stores the picked workspace, fresh-install keeps `''`, re-init restores + re-attaches; note: close the DB handle BEFORE `rmSync` of the temp dir or WAL handles hold it EBUSY). **Gate:** PowerShell + Git Bash — typecheck 0, lint 0, vitest 26/26 (5 files), pytest 3 passed; CDP probe `M22_CDP_OK` 6/6 (chip, menu, `workspace:get` over the bridge, `workspacePath` on session rows, zero renderer console errors; the native dialog itself is not CDP-automatable — its glue is 6 lines delegating to the tested store). **Docs (rule 6, same change):** docs/03 §4 gains the concrete `workspace/*` channel contract + the workspace-state paragraph; docs/06 §7 gains the `workspaces.json` row. **What M2.3 must know:** (1) the sandbox walk's comparison base is `realpathSync(workspaceRoot)` — M2.2 already stores picked paths realpath'd, so `resolveWorkspacePath`'s M2.3 hardening can compare target-realpath against root-realpath without casing surprises; (2) `ToolExecutionContext.workspaceRoot` receives the stored current workspace — the wiring from `workspaces.ts` to the agent loop lands with the first real tool run (M2.4/M2.5), keep the plain-Node boundary (`workspaces.ts` imports no Electron); (3) the M2.1 `testing/harness.ts` + this phase's pattern (vitest for plain-Node stores, esbuild+electron harness for native-ABI proofs) is the established testing toolkit; (4) react-hooks 7 flags setState called (even via async helper) directly inside an effect body — the established renderer pattern is `.then()` callbacks with a `cancelled` flag (M1.3 rule, now in WorkspacePicker too). — ZCode
- 2026-09-06 — **M2.3 done.** Workspace sandbox guard hardened to the full docs/06 §4 form (release-blocking adversarial fixture suite). **Hardening (`sandbox.ts`):** the resolver now runs lexical containment (both `/` and `\`) → realpath walk over every EXISTING component → mode policy → ELOOP refusal → protected-name deny list. Key correctness decisions: (1) existence is probed with `lstatSync`, NOT `existsSync` — `existsSync` FOLLOWS symlinks, so a junction whose target is part of a loop reports false and would hide the loop; `lstatSync` reports the link itself, which is what containment needs; (2) the walk compares each component's realpath against the workspace root's OWN realpath (rootReal), so a user picking a workspace that is itself a junction (OneDrive-redirected) is measured against where it really lives; (3) read mode permits following an outside-pointing LINK (docs/06 §4.3) and, once traversed, the rest of the path stays permitted (`traversedOutside` flag) — writes refuse an escaping link anywhere on the way; (4) ELOOP (symlink/junction loops) and unreadable components become a plain-language refusal, never a hang; (5) protected-name deny list (id_rsa/id_dsa/id_ecdsa/id_ed25519/.ssh/.gnupg/.aws/.netrc/credentials, case-insensitive, single-extension variants like `credentials.json` matched by base name) with a per-path override set (wired to Settings later, default empty). The registry's sandbox stage now passes `tool.access` so read-only tools (M2.4) get the permissive link policy. **Adversarial suite (`sandbox.fixtures.test.ts`, 39 passed + 2 skipped):** real fixture trees in temp dirs — `..` chains (both separators), drive-letter/drive-relative/UNC/device/posix-absolute, junction escapes (write refused / read allowed, including escaping-ancestor-with-nonexistent-leaf), inside-junction allowed both modes, junction LOOPS (two cross-pointing junctions reproduce ELOOP — works without symlink privileges; a self-junction is refused by the OS with EEXIST), Unicode round-trip (Arabic/CJK/emoji), deep/long paths (create-when-allowed else graceful refusal, plus absurd-length clean refusal), Windows case-insensitivity, protected names + override, and a wrapper-integration test proving a `write_file` run through the wrapper refuses a junction escape and writes NOTHING outside (the outside file is untouched). The 2 skipped cases are true-symlink escapes/loops, feature-detected (`canSymlink()`): this box can't create symlinks without Developer Mode, so they're skipped honestly rather than faked. The M2.1 `sandbox.test.ts` was updated to use a real temp root (the hardened guard realpaths the root). **Gate (both shells):** PowerShell + Git Bash — typecheck 0, lint 0, vitest 39 passed + 2 skipped (6 files), pytest 3 passed. **Docs:** no doc change needed — docs/06 §4 already specifies the full policy; the implementation now matches it exactly (the protected-name list contents are an implementation detail of the documented deny list). **What M2.4 must know:** (1) the sandbox is DONE and release-blocking-green — read-only tools (M2.4's `list_dir`/`read_file`/`search_files`) MUST declare `access: 'read'` so they get the permissive link policy; mutating tools (M2.5+) keep `access: 'write'`; (2) `resolveWorkspacePath(root, raw, access, options)` is the single resolver — tools never see raw model paths; (3) the `testing/harness.ts` + esbuild+Electron-harness pattern is the established toolkit (vitest for plain-Node, esbuild bundle run in a real Electron main for native-ABI proofs); (4) `npm test` runs in both shells every sub-phase; (5) the M2.3 checkbox is the last release-blocking safety gate before any file tool — the adversarial suite is the evidence that "it cannot hurt your files" is proven by tests, not vibes (docs/07 §1). — ZCode
- 2026-09-06 — **M2.1–M2.3 independent review pass (clean tree, both shells).** Re-ran every gate from a clean working tree as the session-end verification. **Clean tree:** `git status` clean — exactly the three M2 commits on main (`bac103d` M2.1 / `ec002d6` M2.2 / `0c13692` M2.3), no stray files, no secrets. **PowerShell gates:** `npm run typecheck` exit 0 (node + web), `npm run lint` exit 0, `npm test` → **6 files / 39 passed + 2 skipped** (the 2 skips are the true-symlink-escape and true-symlink-loop cases in `sandbox.fixtures.test.ts`, feature-detected via `canSymlink()` — this Windows box cannot create symlinks without Developer Mode, so they're skipped honestly; the junction-escape, junction-LOOP-via-two-cross-pointing-junctions, Unicode, long/deep, case-insensitivity, protected-name, and wrapper-integration cases all run unconditionally and pass), `uv run pytest` → **3 passed in 0.81s**. **Git Bash gates:** typecheck / lint / test exit 0 each, pytest exit 0. **Boundary audit (the four non-negotiable rules):** (1) `Select-String` over `app/src/main/agent/**/*.ts` for `from 'electron'` → **no matches** — the agent tree is plain Node TS, zero Electron imports; (2) fs-mutation calls (`writeFileSync`/`readFileSync`/`renameSync`/`mkdirSync`/`rmSync`/`unlinkSync`) outside `workspace-fs.ts` → **no matches** — every mutation in the agent tree crosses the registry-injected `WorkspaceFs` facade (AGENTS.md rule 2, structurally enforced); (3) `child_process`/`execSync`/`spawnSync`/`execFile` anywhere under `app/src` → **no matches** — no shell tool, none added (AGENTS.md rule 3); (4) the registry wrapper's `run()` executes the mandated `validate → sandbox → risk → approval-hook → snapshot → execute → truncate` order, proven by the M2.1 stage-order tests (empty stage log on schema/sandbox refusal; `['risk','snapshot']` on risk-1; `['risk','approval','snapshot']` on risk-2). **M2.3 release-blocking evidence:** the adversarial fixture suite is the proof that "it cannot hurt your files" is tested, not vibed (docs/07 §1) — junction escapes refuse writes while allowing reads (docs/06 §4.3), junction loops refuse cleanly without hanging, Unicode/long-path/case edges round-trip, protected names refuse, and the wrapper-integration test confirms a `write_file` run writes NOTHING outside the workspace. **Findings / non-blocking nits for M2.4+:** (1) the M2.2 Electron-ABI harness pattern (esbuild-bundle the repos, run inside a real Electron main) is now the established way to prove DB-touching behavior — vitest cannot load better-sqlite3 on this box; (2) `resolveWorkspacePath` probes existence with `lstatSync` not `existsSync` specifically so junction loops aren't hidden — worth a comment-heavy test if the loop fixture ever needs to move; (3) the protected-name list is hardcoded and conservative; per-path overrides exist but the Settings UI to set them is a later phase; (4) `npm test` now exists and must be run in BOTH shells every sub-phase. The M2 safety foundation is verified and nothing builds on it until this pass was written. — reviewer
- 2026-09-07 - **M2.4 done.** Read-only tools + ask_user + tool cards live in the agent loop. **Tools (`src/main/agent/tools/`, all through the registry wrapper):** `list_dir` (risk 0, read access, "." = root), `read_file` (risk 0, startLine/maxLines paging + truncated flag), `search_files` (risk 0, case-insensitive literal substring + optional glob, match cap with truncated flag), `ask_user` (risk 0, conversation primitive - blocks the loop on the user reply via the synthetic `tool-output-available` pause chunk + `tool:answer` IPC settle; NOT approval-gated by design). **Loop wiring (`src/main/ipc/chat.ts`):** `buildGlobalRegistry()` defines the five tools (M2.1 write_file included) once per app; `toAiSdkTools(ctx)` bridges every call through validate->sandbox->risk->approval->snapshot->execute->truncate with the AI SDK's `toolCallId` threaded to ask_user; per-run `buildRunContext` injects workspaceRoot + sender + ask_user answer registry (resolve/reject per toolCallId, stop rejects pendings); step cap 25 with friendly note; reasoning stripped BOTH at replay (`replayable()`) AND intra-turn (`stripStepReasoning` via `prepareStep` - live-gate finding: step 2+ resends the prior step's reasoning parts, Groq 400s on `reasoning_content`). **Tool cards (`components/cards/`, assistant-ui `setToolUI` registry):** `ToolUIRegistry` mounts once per runtime with a defensive shim (unknown part shapes degrade to `GenericToolCard`, never crash); `BaseToolCard` chrome (plain-language title, status, meta, collapsible); per-tool cards: `ListDirCard`, `ReadFileCard`, `SearchFilesCard`, `AskUserCard` (question + textarea + option chips + error line; flips to Replied with the answered value); unknown tools fall back to `GenericToolCard` (write_file lands its custom card in M2.5). **Preload:** `window.agento.tool.answer({ toolCallId, answer })` -> `tool:answer` IPC (d.ts + runtime in sync). **Live-gate bugs found & fixed (the reason the gate exists):** (1) **ask_user schema rejected `options: null`** - Groq's openai/gpt-oss-120b sends explicit null for open-ended questions and the strict `.optional()` made the provider 400 before our code ever ran; fix: `.nullish().transform(v => v ?? undefined)` keeps the clean `options?: string[]` contract (regression test added). (2) **intra-turn reasoning 400** (above). (3) Driver crashes on "awaiting card vanished" (two drivers raced in one run) - `answerAwaitingCard` now retries the find->type->click cycle. (4) The old "assistant mentions blue" check asserted model wording, violating the gate's own DOM-the-app-owns doctrine - replaced with app-owned proofs (answered-value === 'blue' + Replied flip + assistant text grew + natural finish). **Gate evidence:** m24 CDP driver **24/24** on a fresh DB (list/read/search cards with fixture content; ask_user pauses with Stop visible + no terminal, "blue" answer delivered + Replied flip + text grew 390->418 + natural finish; `window.agento.tool.answer` bridge guard; zero renderer console errors; localhost-only egress); db-dump-m24 ALL CHECKS PASSED (3 migrations, exactly the 4 M1 tables - no new tables, text-only assistant rows, session stamped the fixture workspace, usage recorded only for natural finishes); PowerShell + Git Bash: typecheck 0, lint 0, vitest 71 passed + 2 skipped (13 files), pytest 3 passed. **Transient provider outage mid-session:** Groq "Access denied" (network-level, api.groq.com unreachable ~20 min) failed one gate attempt with zero app-side errors - the app surfaced honest error parts; re-run after connectivity returned went 24/24, so the failure was environmental, not code. **Docs (rule 6, same change):** docs/03 SS4 gains the `tool:answer` channel + the ask_user pause protocol paragraph; docs/02 SS2.1 + docs/04 SS3.1 corrected from the `makeAssistantToolUI` sketch to the shipped `setToolUI` runtime API (the 0.11.56 line deprecates the hook form). **Persistence note:** tool-call turns persist their TEXT (the accumulator keeps text-only parts; tool_calls/checkpoints tables land in M2.5). **What M2.5 must know:** (1) the durable `checkpoints` table (docs/03 SS8 shape: id/session_id/tool_call_id/path/dest_path/existed/content BLOB/size/sha256/before_excerpt/after_excerpt/reverted_at/created_at) + content caps (10 MB/file, 200 MB/session, evict-oldest with warning) + the `tool_calls` audit table land via one drizzle migration (0003); the run ctx's `snapshot()` should write through to it (M2.1's in-memory store swaps for the durable one - the wrapper contract is identical); (2) `create_dir` + `write_file` get plain-language cards + custom before/after excerpt payloads (write_file returns `{ beforeExcerpt, afterExcerpt }` per docs/03 SS5); (3) the ChangesStub view reads checkpoints (session-scoped) - "a create/write run lands a checkpoint the Changes view can see" is the M2.5 acceptance; (4) keep mutations through the wrapper - no new fs paths; (5) the established CDP-driver + gate .ps1 + db-dump pattern (fresh DB, detached-with-redirect for long runs) is reusable as-is. - ZCode
- 2026-09-07 — **M2.7 done.** `move_path` (always risk 2 — the source disappears, so approval always blocks) + `copy_path` (risk 1 new / 2 onto-existing, mirroring `write_file`) + `delete_path` (always risk 3) through the wrapper with snapshots taken. **Judgment calls (docs-ambiguous, recorded here):** (1) docs/03 §5's "2 onto existing" leaves the base case open — move is 2 always (checklist demands approval on every move), copy mirrors write_file, delete is 3 always; (2) `copy_path`/`delete_path` are file-scoped — folder copy and non-empty-dir delete refuse honestly (recursive capture is M5-batch territory; the milestone gate is file-level); (3) move onto an existing dest replaces it (both sides were snapshotted, so undo restores each). **Mechanics:** facade gains `movePath`/`copyFile`→`copyPath`/`deletePath` with the containment backstop (no `fs` in tool bodies); `ToolDefinition.checkpointDestPath` (only move defines it) stores the resolved dest on the FIRST path field's row → `checkpoints.dest_path` via the run-ctx sink; multi-path tools land one row per pathField in order; MovePathCard/CopyPathCard/DeletePathCard on the `setToolUI` slots (absolute paths reduced to file names) + `changes:list` titles; docs/04 §4 logged same change. **What M2.8 must know:** one move call = TWO checkpoint rows sharing a toolCallId (`from` with dest_path + content, `to` with old dest content) — the `checkpointIdsByToolCall` map in `chat.ts` keeps the LAST row per toolCallId, and `created_at` ties are possible (ms precision), so undo-all ordering needs a tiebreak; eviction-nulls-content + `reverted_at` columns are already in the schema awaiting the undo engine — ZCode
- 2026-09-08 — **M2.8 done.** Changes panel + undo engine. **Engine (`agent/undo.ts`, storage-free):** single restore captures the pre-undo state as a new row inheriting the original `toolCallId` (undo-of-undo + feed titles), then moved→rename-back (dest gone → content/dir fallback), created→delete (already-gone = idempotent success), existed-dir→mkdir, existed-file→sha-verified byte restore; evicted (`content null`, no dest) and corrupt rows refuse BEFORE any row is appended (no stray rows, refusal leaves the row retryable); undo-all groups by toolCallId (newest-group-first, oldest-first inside — found live by the matrix: newest-first destroys a move-onto-existing's moved bytes). **Schema:** migration 0004 `is_dir` (eviction-proof: evict-oldest never touches it, so dir rows can't be mistaken for evicted files) + feed ordered by rowid (ms `created_at` ties). **IPC:** `changes:undo`/`changes:undo-all` blocked mid-run via chat's `activeRuns`; `ChangeEntry` gains `groupKey`/`relativeDestPath`/`isDir`; panel groups multi-row mutations into one item whose undo fans out oldest-first, inline confirms, Undo-all confirm, `.changes-panel__list` joined to the scrollbar selectors; docs/03 §4/§8 + docs/04 §4 same change (also reconstructing the M2.5 `changes:list` §4 line lost in the incident below). **Incident:** `git checkout -f backup/full-history` + return wiped the gitignored on-disk docs (tracked on backup, untracked on main) — recovered byte-identical from the backup branch + `docbak` copies (verified: 373 lines, M2.7 tail intact); procedure fix: stage docs copies in temp BEFORE any cross-branch checkout and restore after. **What the M2 gate must know:** durable undo adapter + migration 0004 + panel are proven only in vitest (fake store) — the live CDP demo must exercise real `changes:undo`/`undo-all` against SQLite; approval hook is still auto-approve (M3.2) so the demo drives tools that ask nothing or accepts the dialog shape as-is — ZCode
- 2026-09-08 — **M2.8 Devlog correction (gate-driven, no code change).** The M2.8 line above says undo rows inherit "the original `toolCallId`" — that was the pre-gate belief. The live M2 gate found the "restored ✓" DOM check never fired: inheriting the call id kept the group forever active, so the undone item could never dim. Fix (amended into `a149b47`): undo rows now store **null** `toolCallId` — their own solo group, never merged into the undone call's group — so the panel dims the item with "restored ✓" while the undo row stays active for undo-of-undo. The vitest fake store's `appendUndoRow` mirrors this (null toolCallId). Test count is **122 passed + 2 skipped** (the 14th case, "undo rows are solo groups", asserts `undone?.toolCallId).toBeNull()`). — ZCode
- 2026-09-08 — **M2 milestone gate — GREEN (25/25 CDP + ALL CHECKS PASSED DB dump).** Live CDP run passed 25/25 on attempt 1: copy card rendered (steering held), edit card with Before/After, backup.md held report content + today's date, per-item undo removed the date with "restored ✓" dim, Undo all removed backup.md with report.md byte-identical, zero console errors, localhost-only egress. DB dump: 5 migrations, 6 tables, copy_path + edit_file in tool_calls, 7 checkpoint rows (4 reverted, 3 active undo rows). Gate report: `phase-reports/M2-milestone-gate.md`. — ZCode
- 2026-09-09 — **M3.0 spike DONE — plan mechanism proven on the pinned `ai@5.0.250` (docs/03 §2's "verify the exact tool-choice forcing API" note is now answered).** New regression test `app/src/main/agent/plan-mechanism.spike.test.ts` (2 tests, both passing) drives `streamText` with `MockLanguageModelV2` + `simulateReadableStream` from `ai/test` — NO network, NO real provider — and proves: ① `toolChoice: { type: 'tool', toolName: 'emit_plan' }` is forwarded verbatim to the model call (`doStreamCalls[0].toolChoice` asserted equal — a real provider must honor it, so plan-first is STRUCTURAL, not prompted); ② `stopWhen: [stepCountIs(1)]` ends the loop after the single plan step with exactly one `doStream` call (a second call would exhaust the single scripted turn and fail); ③ the plan tool call flows through `toUIMessageStream()` as a `tool-input-available` part with a terminal `finish` (the exact shape chat.ts forwards and the cards render). Two API facts recorded for M3.1: the provider-v2 `tool-call` stream part carries its arguments as the raw JSON **string** in `input` (`LanguageModelV2ToolCall`, `@ai-sdk/provider` — the SDK parses it against the tool schema), and `MockLanguageModelV2` implements `doStream` ONLY (its `doGenerate` throws "Not implemented"), so all M3 e2e scripting (M3.7 card) must target `streamText`, which is the production loop anyway. **Mechanism decision for M3.1 (docs/03 §2, difference from the doc: none): TWO sequential `streamText` calls — call 1 with `tools: { emit_plan }` + forced toolChoice + `stepCountIs(1)`, block on the plan-start promise, call 2 with the full tool set — not a one-call mid-stream pause.** Gate: `npm run typecheck` 0, `npm run lint` 0, `npm test` 21 files / 128 passed + 2 skipped. No checkbox ticked (spike, not a sub-phase); M3.1 card (`phase-reports/m3-task-cards/02-…`) unblocked.
- 2026-09-08 — **M2 seam-level review (clean tree, both shells).** `npm run typecheck` 0, `npm run lint` 0, `npm test` 20 files / 122 passed + 2 skipped, `uv run pytest` 3 passed. **Invariant audit:** zero tool bodies import `node:fs` (all mutations through `ctx.fs.*`); zero `child_process`/`execSync` under `app/src`; agent tree has zero Electron imports; every M2 tool has a vitest suite; IPC contracts documented. — ZCode
- 2026-09-08 — **M2 seam review follow-up fixes (independent review of M2.1–M2.8, clean tree).** ① **`snapshotFields` contract (docs/03 §5 + §8, same change):** the wrapper snapshots every `pathField` by default, but a tool that does not mutate one of its inputs narrows the set — `copy_path` declares `snapshotFields: () => ['to']` because its source is only *read*. Before this, a copy landed a source row whose undo could rewrite the source to its pre-copy state, silently reverting any unrelated later edit to it; the Changes feed also showed a spurious source item. Move keeps snapshotting both sides (its source IS mutated). New undo-matrix cases prove the guarantee: "undo of a copy never touches the source — a later source edit survives" + "restores the old destination". ② **`walkFiles` no longer follows links:** it used `statSync` (follows) so search could recurse through an outside-pointing junction/symlink into a cycle or out of the workspace; now `lstatSync`-based — links are skipped outright (junction-skip test added; junctions need no privileges on Windows). ③ **`list_dir` card title:** the wrapper hands `describe` the pre-resolved ABSOLUTE path, so the header read `List D:\…\workspace` — now `List <folder-name>` (docs/04 §3.1 path hygiene; test added). ④ **Undo serialized main-side (`ipc/changes.ts`):** two concurrent `changes:undo`/`undo-all` invokes could interleave pre-undo state captures and restores (the engine is not transactional) — a promise chain queues them; the panel disables every row's buttons while any undo is in flight. ⑤ Housekeeping: `SESSION-*.md` added to `.gitignore`. Gate after the changes: `npm run typecheck` 0, `npm run lint` 0, `npm test` 20 files / **126 passed + 2 skipped** (undo 14→16, workspace-fs 5→6, list_dir 3→4), `uv run pytest` 3 passed. — ZCode
- 2026-09-08 — **Known-by-design interim state (review note, no code change):** in the LIVE app the risk ≥ 2 approval hook is still the M2.4 recorded no-op — `context.ts`'s `requestApproval` returns `'approve'` unconditionally — so `edit_file`/`move_path`/`delete_path`/overwrite-`write_file`/copy-onto-existing run with NO dialog today. This is per the roadmap (M3.2 wires the real ApprovalDialog; M3.3 adds coalescing), but it means a delete currently executes silently unless the model narrates first — the M3 seam must land the dialog gate as early as possible, and any free-tier demo of M2's mutators is only safe because of this explicit note. — ZCode
- 2026-09-09 — **M3 code slice DONE in one session (demo-able trust loop; PowerShell gate: typecheck 0, lint 0, vitest 27 files / 153 passed + 2 skipped).** M3.1 gate fix: `plan-run.ts` single-terminal hold semantics kept (test corrected to assert `heldFinish`, not wire finishes) + plan-less degradation now runs execution (`!planEmitted \|\| planApproved`). M3.2: real `requestApproval` pending map + `resolveApproval`/`rejectApprovals` + `approval:respond` IPC (unknown-id → ok:false) + blocking ApprovalDialog + `approval/requested`/`approval/resolved` in the event union (main + renderer + preload). M3.3 STUB (`coalesce.ts` same-tool grouping, 42-calls-1-request test green; DEFERRED: 25% over-projection re-ask, bulk >25 → risk 3, step/shape key narrowing). M3.4: cancel flag via wrapped `requestApproval` consumed by `stopWhen` at step boundaries; skip = SDK continues. M3.5: `verifyStep` client built exactly to docs/05 §2 (4 fake-fetch tests; timeout/non-200/parse → skipped); loop does verify + ONE retry on missed segments; degraded default injected as skipped → honest "not verified"; docs/03 §4 table corrected (`verification/finished` carries `stepId`, not `callId`). M3.6: PlanPanel live `statuses`/`verification`/`errors` + footer N-of-M + CSS badges/dialog. M3.7: smoke `plan-run.e2e.test.ts` green (plan → approve → execute → verify-skipped). DEFERRED (not cut, just open): manual dress rehearsal, deny/cancel/MAX_STEPS e2e matrix, CDP panel probe — M3.7 manual box stays open, Current phase = M3.7. No new deps; agent tree still Electron-free; no shell tool. — ZCode
- 2026-09-09 — **M3.7 rehearsal attempt 1 FAILED at the plan gate (no plan rendered; gate-driven fixes, no checkbox change).** Live Groq `gpt-oss-120b` refuses forced tool choice (`tool_use_failed: Tool choice is required, but model did not call a tool`) — plan-first-structural does not survive this provider. Fix (`plan-run.ts`): Phase 1 now tries forced, then retries once with `toolChoice: 'auto'` over the SAME single-tool set (model can only plan or text); both attempts HOLD error parts so a first-attempt refusal never pollutes the thread. Second finding in the same log: the model breached `search_files.maxResults` (1000 > 200) and the provider killed the turn before our wrapper saw it — `.max()` caps REMOVED from `search_files.maxResults` + `read_file.maxLines` (clamping already lived in `execute`; negative-rejection test still green). Third (found via a scratch test, deleted after): held error chunks mark the accumulator failed, and the refusal branch gated on `!isFailed` ended the run SILENT — gate is now `!terminalSent` only. New tests: forced-refusal fallback (error held off-wire, gate + execution after approve) + double-refusal (honest error, zero execution, 2 doStream calls). Gate after: typecheck 0, lint 0, vitest 27 files / 156 passed + 2 skipped. Rehearsal re-running. — ZCode
- 2026-09-10 — M3 code slice committed as one milestone commit (pre-flight: no lingering dev processes, port 7891 free; gates re-verified on the tree first: typecheck 0, lint 0, vitest 27 files / 162 passed + 2 skipped — count grew from 156 with the plan-gate fix tests, pytest 3 passed). M3.7 manual dress rehearsal is superseded by the MVP's final manual run-through (MVP_PLAN.md). Removed stray root artifacts: empty root package.json + package-lock.json (stray root npm install; app/ is the JS project) and a foreign install.cmd (unrelated "Antigravity CLI" installer that had landed in the workspace root). MVP work (MVP_PLAN.md) starts from this base — ZCode
- 2026-09-10 — **MVP step 1: `/document/extract` live (pypdf 6.18.0 + python-docx 1.2.0 via uv).** Deviation per rule 8: the MVP cut substitutes pypdf/python-docx for Docling **today only** (MVP_PLAN.md's call; STACK.md rows updated in the same change, "do not substitute" row untouched — Docling remains the M4.7 structured-parser decision). Heavy imports lazy per the sidecar convention; 200k-char honest truncation cap; `.pdf`/`.docx`/`.txt`/`.md` only, plain-language 404/422 errors. Tests over the real uvicorn stack (port 7901, stdlib urllib — TestClient still deferred): txt, truncation-at-cap, hand-built valid PDF fixture, python-docx-generated docx with table, 404/422/403. pytest 10 passed — ZCode
- 2026-09-10 — **MVP step 2: `read_document` tool live.** Cloned from `read_file` (risk 0, `access: 'read'`, group 'documents'); degradation ladder: text files read via `ctx.fs` (sidecar-down-safe), `.pdf`/`.docx` via new optional `ctx.documents.extract` (typed `DocumentCapabilities` in types.ts, injected in `chat.ts` from the new `ipc/sidecar-calls.ts` helper over `sidecarFetch`, 15 s timeout) with an honest "needs the intelligence service" error when absent — the wrapper never catches execute exceptions, so the ladder catches and answers `ok:false` with plain language. Cap lesson: the tool's own cap must sit below the wrapper's 8 KB `MAX_TOOL_OUTPUT_BYTES` or the wrapper replaces the whole result and the honest `truncated` marker never ships — capped at 6,000 chars. Card + ToolUIRegistry registration; system prompt unchanged by design (tool descriptions carry the per-tool rules). vitest 169 passed + 2 skipped (7 new), typecheck + lint green — ZCode
- 2026-09-10 — **UI polish: real three-column shell (`UI_POLISH_PLAN.md`).** Root cause fixed, not patched: the Plan/Changes panels were `position: fixed` overlays on a flat `#root` fragment, so chat messages rendered underneath them and a hardcoded `45vh` offset (`.changes-panel--plan-open`) papered over panel-on-panel overlap. Now `main.chat-main` (flex:1, min-width:0) owns the thread and one in-flow `aside.right-rail` (17rem, border-left; rendered only when a plan OR an active session exists) stacks PlanPanel (`flex-shrink:0; max-height:50%`, list scrolls internally) above ChangesPanel (`flex:1; min-height:0`, list `flex:1` so it fills-and-scrolls); `.changes-panel--plan-open` and the `shifted` prop are deleted. Settings + status dot left the floating top-right corner for a `.sessions-footer` on the sidebar (`onOpenSettings` prop added to SessionsSidebar; `SidecarStatusDot` unchanged except its stale "fixed chrome" comment, `.status-dot` lost only its fixed positioning). Sidebar polish only: bordered `+ New chat` (hairline-over-shadows doctrine), tightened session rows, small-caps "Conversations" label; `.thread` max-width 56→48rem (spec §7 ~760px). docs/04 §2/§4 updated (rule 6); no new dependencies, no IPC/preload changes. Gates: typecheck 0, lint 0, vitest 32 files / 196 passed + 2 skipped (no renderer test file exists, so the plan's rail-visibility test item is N/A) — Cline
- 2026-09-10 — **UI polish review pass (independent verifier): PASS + 2 code nits, both fixed.** Verifier confirmed: no fixed layout panels remain (only the three intentional modals), rail condition exactly `plan !== null || activeSessionId !== null`, no design/dep/IPC leaks, gates match (196 + 2 skipped). Fixes from its report: (1) `SidecarStatusDot` header comment still said "Fixed chrome dot" — corrected (it lives in the sidebar footer now); (2) `.right-rail` switched from `<aside aria-label="Plan and changes">` to a plain `<div>` wrapper so PlanPanel/ChangesPanel remain the only complementary landmarks (three nested `<aside>`s would have been a landmark salad). Its third nit — the "— Cline" devlog signature — is a non-issue: `— Cline` already appears in the Devlog (2026-09-05, M1.4 correction entry). The dot-on-blank-loading-screen note is covered by the plan ("dot on blank screen is fine"). Gates re-run after nits: typecheck 0, lint 0, vitest 32 files / 196 passed + 2 skipped — Cline
- 2026-09-10 — **MVP step 3: `summarize_document` tool live.** Extraction reuses the read_document ladder (`document-text.ts`); summarization is a one-shot `generateText` over the run's configured provider/model via new optional `ctx.llm` (`LlmCapabilities` in types.ts, built in `chat.ts` — keys never leave the Electron main process, per the LLM-calls-live-TS-side decision). Hard 12k-char extract cap (MVP-sized files, no chunking), honest `truncated` meta, plain-language refusals when `ctx.llm`/sidecar absent. Card + ToolUIRegistry registration. vitest 174 passed + 2 skipped (5 new), typecheck + lint green — ZCode
- 2026-09-10 — **MVP step 4: `web_fetch` tool live.** Plain HTTP GET via new optional `ctx.web` (Node global fetch in `chat.ts`: 10 s timeout, 2 MB body cap, UTF-8); HTML→text is a dependency-free strip (script/style/comments out, block tags → newlines, common entities, whitespace collapse) + `<title>` extraction per the MVP plan's cut-inside-the-step fallback. Guards: http/https schemes only, non-200 → plain-language error, non-HTML/text/json content types refused, 6k-char honest truncation below the wrapper cap. Card + ToolUIRegistry registration. vitest 181 passed + 2 skipped (7 new), typecheck + lint green — ZCode
- 2026-09-10 — **MVP step 5: semantic search live (standout feature).** Sidecar `POST /embed/embed` over fastembed 0.8.0 (`all-MiniLM-L6-v2`, 384-dim ONNX, lazy module-level `_get_model()` monkeypatched in tests; batch caps 256 texts/400k chars; real-model smoke runs at boot — see warmup). TS `semantic-index.ts`: walks via `ctx.fs.walkFiles` (facade doctrine), text/code ext whitelist + 5 MB cap + 1,800-char windows (≤40/file, ≤5,000 total), cache entries keyed by **content hash** (subsumes the plan's mtime+size — strictly stronger; re-embeds changed files only), JSON cache in **userData**/semantic-index/<sha1(root)>.json (deviation from the plan's `.agento/` note: a risk-0 tool must not write into the workspace — it would bypass the snapshot pipeline); pdf/docx indexed via the extract capability when present, skipped honestly otherwise; cosine ranking in JS. Tool `semantic_search` (risk 0, no pathFields) + card with ranked results, score %, snippets, click → new sandbox-contained `system:open-path` IPC (docs/03 §4 + docs/04 §4 updated same change; preload `system.openPath`). Model warmup: main fires a 1-string embed the first time the sidecar flips healthy so the ~90 MB download never blocks a query. Test gotcha logged: the cache dir must sit OUTSIDE the walked workspace or the walker indexes the cache file itself. vitest 194 passed + 2 skipped (13 new), pytest 16 passed (6 new), typecheck + lint green — ZCode
- 2026-09-10 — **MVP step 6: `/intent/classify` + `/safety/classify` heuristics, audit-only wiring.** Deliberate scope notes: (1) MVP_PLAN step 5 assumed classifier stubs existed in `plan-run.ts` — none do (grep-verified), so there was nothing to "fill in"; (2) per the approved decision, wiring is audit-only and the trust loop is untouched (`registry.ts`/`plan-run.ts` unmodified, `riskSource` stays `rule_table`). Python `heuristics.py`: intent = keyword/regex scoring over document_read/web_research/organize/file_operation with multi_step on cross-category conflict and honest `unsure` fallback, confidence literal always `"heuristic"`; safety = deny-list first (traversal, system dirs, credential locations, bulk >25 paths → risk 3) then rule-table mirror (read-only→0, create→1, overwrite/move/copy→2, delete→3). TS wiring in `chat.ts` only: fire-and-forget `/intent/classify` per send (logged) + `/safety/classify` per tool outcome (logged on disagreement with the rule table) — never blocking, silent degrade. pytest 31 passed (15 new), vitest/typecheck/lint green — ZCode
- 2026-09-10 — **MVP step 7: `/completion/verify` live + M3.5's stub verifier replaced.** Python `verify.py`: heuristic postcondition checks over the actions' sandbox-resolved ABSOLUTE paths (create/write/edit/copy → target exists; move → dest exists + source gone; delete → absent; relative paths and read-only tools carry no check), score = passed/checked, plain-language `missed_segments`, nothing-checkable → honest 1.0/true. The endpoint implements the SHIPPED TS client's flattened contract (`{instruction, step_description, actions, before_after}` → `{completion_score, is_complete, missed_segments}`) — docs/05's nested example is reconciled to the client in the docs pass (rule 6). TS: M3.5's `verify: skipped` stub in `chat.ts` replaced by `verifyStep` over `sidecarFetch` (8 s timeout); the audit sink feeds a per-run action list and the new `hasMutatingActions` gate (unit-tested) skips read-only-only runs so badges stay honest in both directions; the one retry re-checks the full action set (no drain). pytest 40 passed (9 new), vitest 196 passed + 2 skipped (2 new), typecheck + lint green — ZCode
- 2026-09-10 — **MVP step 8: onboarding screen live.** One static full-screen gate (`Onboarding.tsx` + `onboarding*` CSS block, reusing the settings-* field classes): welcome → pick a workspace folder (`workspaces.pick` IS the setter main-side) → provider select + API key entry (same settings IPC as SettingsDialog; plaintext dropped from renderer state on save). App gate: on mount reads `settings.get()` + `workspaces.get()`, shows the screen until `hasKey && current` — null-check renders the status dot only (blank beats a chat-UI flash), a failed check degrades to the main UI which already handles both missing states honestly. No new IPC channels. vitest 196 passed + 2 skipped, typecheck + lint green — ZCode
- 2026-09-10 — **MVP step 9 / final pass: docs + tracker + boot smoke.** docs/05 gained the dated "MVP cut (2026-09-10)" section (rule 6): the five live endpoints' shapes, the pypdf/python-docx substitution recorded, `/document/summarize` documented as non-existent (summarization is TS-side — keys never reach Python), and the stale nested `/completion/verify` request example corrected to the flattened shape the shipped TS client sends. Current phase updated; shipped-vs-deferred table above. Boot smoke: `npm run dev` launched clean — main/preload built, DB migrated through 0005, sidecar healthy on 7891, zero log errors — and closed via the documented graceful `taskkill /PID` with zero stray processes and the port freed. Final gates: typecheck 0, lint 0, vitest 32 files / 196 passed + 2 skipped (40 new tests across the MVP), pytest 40 passed (37 new). Real-model fastembed download (~90 MB) runs in the background; the first semantic search or the dev-boot warmup completes it if not — ZCode
- 2026-09-10 — **Real model verified; network workaround landed.** The HuggingFace file CDN hangs indefinitely on this machine (hub API answers 200 in ~2 s but file downloads stall with no error — so fastembed's GCS fallback never fires, since a hang is not an exception). Fix, human-seeded: the user downloaded fastembed's own GCS mirror (`storage.googleapis.com/qdrant-fastembed/sentence-transformers-all-MiniLM-L6-v2.tar.gz`, verified fast) and the archive was extracted to `<userData>/fastembed/fast-all-MiniLM-L6-v2/` (fastembed 0.8's default cache is `%TEMP%\fastembed_cache` — a temp cleaner would delete the model, so the sidecar spawn now pins `FASTEMBED_CACHE_PATH` to a durable app-owned dir, passed in from `index.ts` keeping `sidecar.ts` Electron-free per rule 1, plus `HF_HUB_OFFLINE=1` so the sidecar never attempts the hanging HF path; a fresh machine must seed the cache once or `semantic_search` answers 503 honestly). Real-model smoke: staged diagnostic ran import → `TextEmbedding` from cache → embed in ~20 s cold, **384-dim vectors confirmed**. Note for M4: fastembed's own warmup in the app is superseded by this seeding on THIS machine; the `warmEmbeddingModel` boot call remains harmless (instant cache hit now). All gates re-verified after the sidecar.ts/index.ts change — ZCode
- 2026-09-10 — **Explicit Plan/Act composer tabs (functional, not a chip).** Per-session `sessions.mode` (`plan`|`act`, default `act`; migration 0006 + legacy `auto` normalization) owned main-side: `session:create { mode? }`, `session:set-mode`, `session:plan` (latest saved plan, restored on session open), `chat:send` routes on the persisted mode. **Plan** (`runPlanModeTurn`) is structurally read-only — discovery offers read-access tools only, then a forced `emit_plan`; write tools never reach the model and no execution path exists; the panel shows a read-only chip + Act-handoff footer. **Act** (`runActTurn`) executes with the full registry minus `emit_plan` (wrapper/approval/snapshot guarantees unchanged); "go ahead" carries out the saved plan as advisory context (`isGoAheadMessage`). Legacy `plan/start` kept as an honest no-op. Renderer: Plan/Act tablist above the composer input (disabled mid-run), mode-aware placeholders/status copy, no-diff/no-shell rules held. Same-commit doc updates: docs/03 §2/§4/§5/§8 + docs/04 §2/§3.3/§3.5. Tests: new `plan-act-modes.test.ts` (go-ahead matrix, registry include/exclude filter, Act excludes emit_plan, Plan discovery read-only + plan emitted); storage/IPC routing untestable under vitest (better-sqlite3 is Electron-ABI). — Muse Spark
- 2026-09-10 — **Workspace-aware system prompt fix.** Root cause: the prompt was a static constant that never named the workspace, while WORKFLOW step 1 told the model to ask for one when none is set — so it asked even with a folder picked in the sidebar. `app/src/main/ipc/system-prompt.ts` now exports `buildSystemPrompt(workspaceRoot: string | null)` (base text verbatim; with a workspace it inserts a WORKSPACE section naming the folder + "pass . for the root" and drops the ask clause; null keeps the old ask behavior); `app/src/main/ipc/chat.ts` passes `workspaceRoot || null` per run. New `system-prompt.test.ts` (3 tests); docs/03 section 9 + section 2 pseudocode updated same commit (rule 6). Gates: typecheck 0, lint 0, vitest 33 files / 206 passed + 2 skipped — Cline
- 2026-09-10 — **Trust-boundary hardening slice (audit follow-through).** Fail-closed durable snapshots (snapshot throw refuses the mutation, no un-undoable writes) + binary-safe snapshot/undo (`b64:` marker, 10 MB/file cap enforced, text column untouched); TOCTOU revalidation (`assertAbsoluteInsideWorkspace` + per-call `revalidate`, atomic buffer writes with post-mkdir recheck); session/run/step binding (`plan:start` sessionId+runId, approval group key `tool:risk:stepId`, bulk >25 → risk 3, session-bound workspace, step-mapped outcomes, renderer seq/run filtering); honest verification (resolved absolute inputs in `audit.resolvedInput`, last-user-message instruction, `checked==0` + mutating actions → 0.0/False); SSRF guard (`web-fetch-guard.ts`: IP-literal + DNS-resolved loopback/private/link-local/multicast refusal, redirect revalidation) + `will-navigate`/`will-redirect` block + `wrapUntrusted` framing on file/document/search/web outputs with assistant-addressed-text flag; sidecar `embed.py` `_lock` → `RLock` (nested acquire deadlocked the real path) + empty-batch early return, `extract.py` source guards (20 MB file / 500-page / 20k-block caps → honest 413, early stop past the char cap); approval dialog becomes a true blocking overlay (no click/Esc dismiss, Esc no longer stops the run under it, autofocus) + window `minWidth 960`/`minHeight 640` + onboarding Enter-to-save and `role=alert` errors. docs/05 extract row updated same change (rule 6). Gates: typecheck 0, lint 0, vitest 35 files / 218 passed + 2 skipped (new: SSRF guard suite), pytest 42 passed (new: 413 oversize + empty-embed-no-model tests) — Muse Spark
- 2026-09-10 � **Settings dialog tabbed by category.** Four tabs (Providers / Appearance / Permissions / Data) via a ole=tablist + 	abpanel panels; inactive panels stay mounted with hidden (plus a .settings-section[hidden] CSS re-assert since the flex class overrides the UA rule) so custom-provider form drafts survive tab switches; dialog resets to Providers on open; notices/errors stay visible below the panels. docs/04 �3.7 updated same change. Gates: typecheck 0, lint 0, vitest 39 files / 257 passed + 2 skipped � Muse Spark
- 2026-09-10 — **User-reported fix batch: sidebar truthfulness + real chat titles.** (1) Session rows no longer render the "N in · N out" token line (totals stay recorded in usage_events + SessionInfo — display only); (2) thread welcome copy replaced ("Say something — the main process echoes it back over IPC" was stale M0 canned-stream text); (3) auto chat titles: first chat:send on a session fires a background one-shot generateText (new src/main/agent/title.ts, injected completer, title in the message's own language) and persists via new title-only setSessionTitle (no updated_at bump); new session/title_updated agent:event (main Zod + renderer mirror + preload union, docs/03 §4 same commit) patches the sidebar row pre-seq-guard; deriveTitle stays as the fallback; existing sessions never re-titled; (4) the "previous chats moved folder" report was display-only: per-session workspace_path was always bound correctly in the DB — sidebar rows now show their bound folder's name (basename; tooltip keeps the full path) and the workspace chip tooltip states it applies to new chats. Gates: typecheck 0, lint 0, vitest 40 files / 268 passed + 2 skipped (new: title.test.ts, 11 tests) — ZCode
- 2026-09-11 — **Demo-readiness slice: QuickActions chips, session rename/delete, global Ctrl+Z/Ctrl+N, composer model chip, cleaner home.** Four visible UI additions for the recorded demo (docs/07 §4.1, written this slice): (1) QuickActions chips on the empty thread — three example prompts that fill (never send) the composer via `useThreadRuntime().composer.setText`, rendered under the composer inside `ThreadPrimitive.If empty`; (2) per-session rename + delete in the sidebar — full stack: `sessions.title_renamed` column (drizzle migration 0007_sessions-title-renamed; journal tag renamed to match, project precedent), `renameSession`/`deleteSession` in the storage repo (delete removes messages/usage/tool_calls/plan_steps/checkpoints explicitly in one transaction — no FK cascades in the schema), `session:rename`/`session:delete` IPC (delete refuses mid-run via `isSessionRunActive`, same doctrine as undo), preload + `index.d.ts` types, hover "…" menu with inline rename editor and delete confirm (warns undo history goes too); the background auto-title now re-reads the row at write time and skips user-renamed sessions (`chat.ts`); (3) global shortcuts per docs/04 §7 — Ctrl+Z undoes the NEWEST change group (groupKey-aware, oldest-first fan-out like ChangesPanel's undoGroup) and Ctrl+N starts a new chat; both renderer-side keydown, fall through inside inputs/textareas/contenteditable so native text undo keeps working, and stay disabled under the settings/approval overlays; (4) model chip in the composer — provider · model label with an UPWARD-opening popover (chip sits at the window bottom; a downward menu ran off-screen) listing the active provider's models + all providers, switching via the existing `settings:set-model`/`settings:set-provider`; sibling of the `role="tablist"`, never inside it. Home de-bloat per user reference screenshot: welcome is now only the folder chip (inline SVG folder glyph, no icon dep) with the composer vertically centered via `.thread:has(.thread-welcome)`; the h1 + lede are gone; QuickActions restyled as ghost pills. New throwaway demo tool: `demo/make_fixture.py` (run via the sidecar env: `cd services/intelligence && uv run python ../../demo/make_fixture.py`) — builds the 42-file messy Downloads folder (12 invoice PDFs via pypdf, 6 .docx via python-docx, 12 text/md, 6 PNGs, 6 duplicates/strays). Gates: typecheck 0 (node+web), lint 0 (after `eslint --fix` prettier pass), vitest 40 files / 268 passed + 2 skipped. No new dependencies → no STACK.md rows — ZCode
- 2026-09-11 — **ask_user replies move from the card into the main composer.** User request: the Ask card's embedded reply form (textarea + Send + option chips) is gone from the message body. While a question pends the run, the composer switches to reply mode: new `chat/ask.ts` `findPendingAsk` derives the open ask from the newest tool part still carrying `__agentoAskUser` (gated on runActive so a dead ask can't trap the composer); `ReplyComposer` in App.tsx renders option chips (when the question carries options) + textarea + Send over the SAME `tool:answer` channel — zero main-process/IPC changes; Enter sends, Shift+Enter newlines, IME-composition guarded; Stop keeps its meaning (reject pending answer + abort); a stale-question refusal shows under the send row with the draft preserved. The Ask card is now display-only (Awaiting your reply / Replied / Stopped before reply + the "Your reply:" record); the status line reads "Waiting for your reply..."; composer swaps back automatically when the answer chunk overwrites the part's output. Dead `.tool-card__form/__options/__chip/__textarea/__form-row/__send` CSS removed (kept `__error` for GenericToolCard), `.composer-replies/.composer-chip/.composer-reply-error` added. docs/03 §7 + docs/04 §3.5 updated same commit. Gates: typecheck 0 (node+web), lint 0, vitest 40 files / 268 passed + 2 skipped — ZCode
- 2026-09-11 — **ask_user reserved for task-blocking questions (user-reported: the model fired it on "hey").** The greeting turned into an Ask card + composer reply mode — the model had no guidance that casual talk is a plain text reply. Two LLM-facing surfaces tightened: the ask_user tool description now says use ONLY when the answer is needed to proceed with a task (never greetings/small talk — answer those as plain text), and WORKFLOW step 1 in the system prompt gained "Greetings and casual conversation get a normal text reply — reserve ask_user for questions you need answered to do the task" (in both the workspace and no-workspace variants, replace strings kept in sync). New system-prompt test asserts the guard in both variants; docs/03 §5 registry row + §9 verbatim prompt updated same commit. Gates: typecheck 0 (node+web), lint 0, vitest 40 files / 269 passed + 2 skipped — ZCode
- 2026-09-11 — **Emoji ban in model-visible text (user request).** System prompt LANGUAGE gained "Never use emojis in a reply — plain text only." (both workspace variants, docs/03 §9 synced). Session titles get the same treatment twice: the title prompt now says "no emojis", and `sanitizeTitle` strips emoji codepoints deterministically (Extended_Pictographic + skin-tone/regional-indicator modifiers, ZWJ, VS16 in an alternation — no-misleading-character-class forbids bare ZWJ/VS16 inside a character class), falling back when a title was emojis-only. New tests: emoji ban in both prompt variants; emoji/modifier stripping + emoji-only fallback in sanitizeTitle; title-prompt "no emojis" assertion. Gates: typecheck 0 (node+web), lint 0, vitest 40 files / 272 passed + 2 skipped — ZCode

- 2026-09-11 � **Composer file refs + Settings badge + contextual prompts (user-reported gaps).** (1) workspace:list-files IPC (sandboxed walkFiles, relpaths only, 1000-cap; session workspace else current; empty/no-workspace resolves empty) + preload pair; (2) FileAttach (paperclip + @-mention, one picker, removable chips, transport appends 'Context files: @rel' reference text, App-owned state into transport props, disabled mid-run/reply-mode); (3) Settings dot only when a built-in provider lacks a key (custom keyless never badges; tooltip + per-provider visit-dismiss); (4) QuickActions scan (list-files 500 + session titles, PDF/organize/pricing rules, generic fallbacks; thresholds mirrored in Electron-free main/agent/workspace-listing.ts with tests). docs/03 �4 + docs/04 �3.5/�3.7/�4/table same change. Gates: typecheck 0, lint 0, vitest 41 files / 278 passed + 2 skipped (new: workspace-listing 6) � Muse Spark
